/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ({

/***/ "../bpl-tools/Components/Mask/assets/shapes/blob.svg":
/*!***********************************************************!*\
  !*** ../bpl-tools/Components/Mask/assets/shapes/blob.svg ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ReactComponent: () => (/* binding */ SvgBlob),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
var _path;
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }

var SvgBlob = function SvgBlob(props) {
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", _extends({
    xmlns: "http://www.w3.org/2000/svg",
    width: 121,
    height: 89
  }, props), _path || (_path = /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    d: "M100.016 7.397c-9.77-6.177-26.708-11.063-35.59-3.684-7.442 6.177-2.38 15.204-9.416 21.59-10.6 9.614-28.159-5.442-41.858 2.632C.676 35.255-4.28 59.149 4.278 73.798c13.137 22.524 53.13 13.854 61.194 12.106 4.5-.993 53.495-12.265 55.469-42.705.957-14.946-9.841-28.78-20.925-35.802"
  })));
};

/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "../bpl-tools/Components/Mask/assets/shapes/circle.svg":
/*!*************************************************************!*\
  !*** ../bpl-tools/Components/Mask/assets/shapes/circle.svg ***!
  \*************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ReactComponent: () => (/* binding */ SvgCircle),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
var _circle;
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }

var SvgCircle = function SvgCircle(props) {
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", _extends({
    xmlns: "http://www.w3.org/2000/svg",
    width: 89,
    height: 89
  }, props), _circle || (_circle = /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("circle", {
    cx: 44.5,
    cy: 44.5,
    r: 44.5
  })));
};

/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,PHN2ZyB4bWxucz0iaHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmciIHdpZHRoPSI4OSIgaGVpZ2h0PSI4OSIgdmlld0JveD0iMCAwIDg5IDg5Ij48Y2lyY2xlIGN4PSI0NC41IiBjeT0iNDQuNSIgcj0iNDQuNSIvPjwvc3ZnPg==");

/***/ }),

/***/ "../bpl-tools/Components/Mask/assets/shapes/flower.svg":
/*!*************************************************************!*\
  !*** ../bpl-tools/Components/Mask/assets/shapes/flower.svg ***!
  \*************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ReactComponent: () => (/* binding */ SvgFlower),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
var _path;
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }

var SvgFlower = function SvgFlower(props) {
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", _extends({
    xmlns: "http://www.w3.org/2000/svg",
    width: 89,
    height: 89
  }, props), _path || (_path = /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    d: "m76.133 44.475 6.33-6.328a22.349 22.349 0 0 0-31.613-31.6l-6.318 6.318-6.372-6.318a22.352 22.352 0 1 0-31.612 31.61l6.318 6.318-6.318 6.372a22.352 22.352 0 0 0 31.612 31.61l6.318-6.318 6.372 6.253a22.349 22.349 0 0 0 31.612-31.6Z"
  })));
};

/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "../bpl-tools/Components/Mask/assets/shapes/hexagon.svg":
/*!**************************************************************!*\
  !*** ../bpl-tools/Components/Mask/assets/shapes/hexagon.svg ***!
  \**************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ReactComponent: () => (/* binding */ SvgHexagon),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
var _path;
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }

var SvgHexagon = function SvgHexagon(props) {
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", _extends({
    xmlns: "http://www.w3.org/2000/svg",
    width: 103,
    height: 89
  }, props), _path || (_path = /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    d: "M77.211 0H25.752L0 44.505 25.752 89h51.46L103 44.505ZM61.77 62.261H41.222L30.939 44.504 41.222 26.74h20.547l10.283 17.766Z"
  })));
};

/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "../bpl-tools/Components/Mask/assets/shapes/sketch.svg":
/*!*************************************************************!*\
  !*** ../bpl-tools/Components/Mask/assets/shapes/sketch.svg ***!
  \*************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ReactComponent: () => (/* binding */ SvgSketch),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
var _path;
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }

var SvgSketch = function SvgSketch(props) {
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", _extends({
    xmlns: "http://www.w3.org/2000/svg",
    width: 91.114,
    height: 89
  }, props), _path || (_path = /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    d: "m82.175 37.527 1.612-1.612c.948-.949-6.37-5.931-7.05-6.378a45 45 0 0 0-5.776-3.325c-.247-.115-.602-.3-.987-.478q1.781-1.897 3.586-3.771c.949-.98-6.363-5.923-7.05-6.37a44 44 0 0 0-5.76-3.332l-.293-.147 1.396-1.542c.94-1.05-6.332-5.9-7.05-6.37a44 44 0 0 0-5.76-3.34c-.54-.247-2.314-1.265-2.847-.687q-13.458 14.993-28.08 28.891c-7.844 7.45-16.567 15.58-18.11 26.87-.115.825 1.327 1.974 1.744 2.314a41 41 0 0 0 5.314 4.057 44 44 0 0 0 5.76 3.34c.64.293 2.176 1.203 2.847.686l2.074-1.558c1.45 1.859 5.97 4.72 6.671 5.175l1.489.956a257 257 0 0 0-4.435 5.622c-.91 1.188 6.263 5.862 7.05 6.37a44 44 0 0 0 5.76 3.332c.602.278 2.23 1.234 2.846.694l9.255-8.098c.825 2.568 14.569 11.63 15.672 9.972L91.06 45.362c.632-.948-4.335-5.036-8.885-7.835"
  })));
};

/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "../bpl-tools/Components/Mask/assets/shapes/triangle.svg":
/*!***************************************************************!*\
  !*** ../bpl-tools/Components/Mask/assets/shapes/triangle.svg ***!
  \***************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ReactComponent: () => (/* binding */ SvgTriangle),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
var _path;
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }

var SvgTriangle = function SvgTriangle(props) {
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", _extends({
    xmlns: "http://www.w3.org/2000/svg",
    width: 103,
    height: 89
  }, props), _path || (_path = /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    d: "M77.255 44.505 51.5 0 25.755 44.505 0 89h103z"
  })));
};

/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,PHN2ZyB4bWxucz0iaHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmciIHdpZHRoPSIxMDMiIGhlaWdodD0iODkiIHZpZXdCb3g9IjAgMCAxMDMgODkiPjxwb2x5Z29uIHBvaW50cz0iNzcuMjU1IDQ0LjUwNSA1MS41IDAgMjUuNzU1IDQ0LjUwNSAwIDg5IDUxLjUgODkgMTAzIDg5IDc3LjI1NSA0NC41MDUiLz48L3N2Zz4=");

/***/ }),

/***/ "../bpl-tools/node_modules/immer/dist/immer.mjs":
/*!******************************************************!*\
  !*** ../bpl-tools/node_modules/immer/dist/immer.mjs ***!
  \******************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   Immer: () => (/* binding */ Immer2),
/* harmony export */   applyPatches: () => (/* binding */ applyPatches),
/* harmony export */   castDraft: () => (/* binding */ castDraft),
/* harmony export */   castImmutable: () => (/* binding */ castImmutable),
/* harmony export */   createDraft: () => (/* binding */ createDraft),
/* harmony export */   current: () => (/* binding */ current),
/* harmony export */   enableMapSet: () => (/* binding */ enableMapSet),
/* harmony export */   enablePatches: () => (/* binding */ enablePatches),
/* harmony export */   finishDraft: () => (/* binding */ finishDraft),
/* harmony export */   freeze: () => (/* binding */ freeze),
/* harmony export */   immerable: () => (/* binding */ DRAFTABLE),
/* harmony export */   isDraft: () => (/* binding */ isDraft),
/* harmony export */   isDraftable: () => (/* binding */ isDraftable),
/* harmony export */   nothing: () => (/* binding */ NOTHING),
/* harmony export */   original: () => (/* binding */ original),
/* harmony export */   produce: () => (/* binding */ produce),
/* harmony export */   produceWithPatches: () => (/* binding */ produceWithPatches),
/* harmony export */   setAutoFreeze: () => (/* binding */ setAutoFreeze),
/* harmony export */   setUseStrictShallowCopy: () => (/* binding */ setUseStrictShallowCopy)
/* harmony export */ });
// src/utils/env.ts
var NOTHING = Symbol.for("immer-nothing");
var DRAFTABLE = Symbol.for("immer-draftable");
var DRAFT_STATE = Symbol.for("immer-state");

// src/utils/errors.ts
var errors =  true ? [
  // All error codes, starting by 0:
  function(plugin) {
    return `The plugin for '${plugin}' has not been loaded into Immer. To enable the plugin, import and call \`enable${plugin}()\` when initializing your application.`;
  },
  function(thing) {
    return `produce can only be called on things that are draftable: plain objects, arrays, Map, Set or classes that are marked with '[immerable]: true'. Got '${thing}'`;
  },
  "This object has been frozen and should not be mutated",
  function(data) {
    return "Cannot use a proxy that has been revoked. Did you pass an object from inside an immer function to an async process? " + data;
  },
  "An immer producer returned a new value *and* modified its draft. Either return a new value *or* modify the draft.",
  "Immer forbids circular references",
  "The first or second argument to `produce` must be a function",
  "The third argument to `produce` must be a function or undefined",
  "First argument to `createDraft` must be a plain object, an array, or an immerable object",
  "First argument to `finishDraft` must be a draft returned by `createDraft`",
  function(thing) {
    return `'current' expects a draft, got: ${thing}`;
  },
  "Object.defineProperty() cannot be used on an Immer draft",
  "Object.setPrototypeOf() cannot be used on an Immer draft",
  "Immer only supports deleting array indices",
  "Immer only supports setting array indices and the 'length' property",
  function(thing) {
    return `'original' expects a draft, got: ${thing}`;
  }
  // Note: if more errors are added, the errorOffset in Patches.ts should be increased
  // See Patches.ts for additional errors
] : 0;
function die(error, ...args) {
  if (true) {
    const e = errors[error];
    const msg = typeof e === "function" ? e.apply(null, args) : e;
    throw new Error(`[Immer] ${msg}`);
  }
  // removed by dead control flow

}

// src/utils/common.ts
var getPrototypeOf = Object.getPrototypeOf;
function isDraft(value) {
  return !!value && !!value[DRAFT_STATE];
}
function isDraftable(value) {
  if (!value)
    return false;
  return isPlainObject(value) || Array.isArray(value) || !!value[DRAFTABLE] || !!value.constructor?.[DRAFTABLE] || isMap(value) || isSet(value);
}
var objectCtorString = Object.prototype.constructor.toString();
function isPlainObject(value) {
  if (!value || typeof value !== "object")
    return false;
  const proto = getPrototypeOf(value);
  if (proto === null) {
    return true;
  }
  const Ctor = Object.hasOwnProperty.call(proto, "constructor") && proto.constructor;
  if (Ctor === Object)
    return true;
  return typeof Ctor == "function" && Function.toString.call(Ctor) === objectCtorString;
}
function original(value) {
  if (!isDraft(value))
    die(15, value);
  return value[DRAFT_STATE].base_;
}
function each(obj, iter) {
  if (getArchtype(obj) === 0 /* Object */) {
    Reflect.ownKeys(obj).forEach((key) => {
      iter(key, obj[key], obj);
    });
  } else {
    obj.forEach((entry, index) => iter(index, entry, obj));
  }
}
function getArchtype(thing) {
  const state = thing[DRAFT_STATE];
  return state ? state.type_ : Array.isArray(thing) ? 1 /* Array */ : isMap(thing) ? 2 /* Map */ : isSet(thing) ? 3 /* Set */ : 0 /* Object */;
}
function has(thing, prop) {
  return getArchtype(thing) === 2 /* Map */ ? thing.has(prop) : Object.prototype.hasOwnProperty.call(thing, prop);
}
function get(thing, prop) {
  return getArchtype(thing) === 2 /* Map */ ? thing.get(prop) : thing[prop];
}
function set(thing, propOrOldValue, value) {
  const t = getArchtype(thing);
  if (t === 2 /* Map */)
    thing.set(propOrOldValue, value);
  else if (t === 3 /* Set */) {
    thing.add(value);
  } else
    thing[propOrOldValue] = value;
}
function is(x, y) {
  if (x === y) {
    return x !== 0 || 1 / x === 1 / y;
  } else {
    return x !== x && y !== y;
  }
}
function isMap(target) {
  return target instanceof Map;
}
function isSet(target) {
  return target instanceof Set;
}
function latest(state) {
  return state.copy_ || state.base_;
}
function shallowCopy(base, strict) {
  if (isMap(base)) {
    return new Map(base);
  }
  if (isSet(base)) {
    return new Set(base);
  }
  if (Array.isArray(base))
    return Array.prototype.slice.call(base);
  const isPlain = isPlainObject(base);
  if (strict === true || strict === "class_only" && !isPlain) {
    const descriptors = Object.getOwnPropertyDescriptors(base);
    delete descriptors[DRAFT_STATE];
    let keys = Reflect.ownKeys(descriptors);
    for (let i = 0; i < keys.length; i++) {
      const key = keys[i];
      const desc = descriptors[key];
      if (desc.writable === false) {
        desc.writable = true;
        desc.configurable = true;
      }
      if (desc.get || desc.set)
        descriptors[key] = {
          configurable: true,
          writable: true,
          // could live with !!desc.set as well here...
          enumerable: desc.enumerable,
          value: base[key]
        };
    }
    return Object.create(getPrototypeOf(base), descriptors);
  } else {
    const proto = getPrototypeOf(base);
    if (proto !== null && isPlain) {
      return { ...base };
    }
    const obj = Object.create(proto);
    return Object.assign(obj, base);
  }
}
function freeze(obj, deep = false) {
  if (isFrozen(obj) || isDraft(obj) || !isDraftable(obj))
    return obj;
  if (getArchtype(obj) > 1) {
    obj.set = obj.add = obj.clear = obj.delete = dontMutateFrozenCollections;
  }
  Object.freeze(obj);
  if (deep)
    Object.entries(obj).forEach(([key, value]) => freeze(value, true));
  return obj;
}
function dontMutateFrozenCollections() {
  die(2);
}
function isFrozen(obj) {
  return Object.isFrozen(obj);
}

// src/utils/plugins.ts
var plugins = {};
function getPlugin(pluginKey) {
  const plugin = plugins[pluginKey];
  if (!plugin) {
    die(0, pluginKey);
  }
  return plugin;
}
function loadPlugin(pluginKey, implementation) {
  if (!plugins[pluginKey])
    plugins[pluginKey] = implementation;
}

// src/core/scope.ts
var currentScope;
function getCurrentScope() {
  return currentScope;
}
function createScope(parent_, immer_) {
  return {
    drafts_: [],
    parent_,
    immer_,
    // Whenever the modified draft contains a draft from another scope, we
    // need to prevent auto-freezing so the unowned draft can be finalized.
    canAutoFreeze_: true,
    unfinalizedDrafts_: 0
  };
}
function usePatchesInScope(scope, patchListener) {
  if (patchListener) {
    getPlugin("Patches");
    scope.patches_ = [];
    scope.inversePatches_ = [];
    scope.patchListener_ = patchListener;
  }
}
function revokeScope(scope) {
  leaveScope(scope);
  scope.drafts_.forEach(revokeDraft);
  scope.drafts_ = null;
}
function leaveScope(scope) {
  if (scope === currentScope) {
    currentScope = scope.parent_;
  }
}
function enterScope(immer2) {
  return currentScope = createScope(currentScope, immer2);
}
function revokeDraft(draft) {
  const state = draft[DRAFT_STATE];
  if (state.type_ === 0 /* Object */ || state.type_ === 1 /* Array */)
    state.revoke_();
  else
    state.revoked_ = true;
}

// src/core/finalize.ts
function processResult(result, scope) {
  scope.unfinalizedDrafts_ = scope.drafts_.length;
  const baseDraft = scope.drafts_[0];
  const isReplaced = result !== void 0 && result !== baseDraft;
  if (isReplaced) {
    if (baseDraft[DRAFT_STATE].modified_) {
      revokeScope(scope);
      die(4);
    }
    if (isDraftable(result)) {
      result = finalize(scope, result);
      if (!scope.parent_)
        maybeFreeze(scope, result);
    }
    if (scope.patches_) {
      getPlugin("Patches").generateReplacementPatches_(
        baseDraft[DRAFT_STATE].base_,
        result,
        scope.patches_,
        scope.inversePatches_
      );
    }
  } else {
    result = finalize(scope, baseDraft, []);
  }
  revokeScope(scope);
  if (scope.patches_) {
    scope.patchListener_(scope.patches_, scope.inversePatches_);
  }
  return result !== NOTHING ? result : void 0;
}
function finalize(rootScope, value, path) {
  if (isFrozen(value))
    return value;
  const state = value[DRAFT_STATE];
  if (!state) {
    each(
      value,
      (key, childValue) => finalizeProperty(rootScope, state, value, key, childValue, path)
    );
    return value;
  }
  if (state.scope_ !== rootScope)
    return value;
  if (!state.modified_) {
    maybeFreeze(rootScope, state.base_, true);
    return state.base_;
  }
  if (!state.finalized_) {
    state.finalized_ = true;
    state.scope_.unfinalizedDrafts_--;
    const result = state.copy_;
    let resultEach = result;
    let isSet2 = false;
    if (state.type_ === 3 /* Set */) {
      resultEach = new Set(result);
      result.clear();
      isSet2 = true;
    }
    each(
      resultEach,
      (key, childValue) => finalizeProperty(rootScope, state, result, key, childValue, path, isSet2)
    );
    maybeFreeze(rootScope, result, false);
    if (path && rootScope.patches_) {
      getPlugin("Patches").generatePatches_(
        state,
        path,
        rootScope.patches_,
        rootScope.inversePatches_
      );
    }
  }
  return state.copy_;
}
function finalizeProperty(rootScope, parentState, targetObject, prop, childValue, rootPath, targetIsSet) {
  if ( true && childValue === targetObject)
    die(5);
  if (isDraft(childValue)) {
    const path = rootPath && parentState && parentState.type_ !== 3 /* Set */ && // Set objects are atomic since they have no keys.
    !has(parentState.assigned_, prop) ? rootPath.concat(prop) : void 0;
    const res = finalize(rootScope, childValue, path);
    set(targetObject, prop, res);
    if (isDraft(res)) {
      rootScope.canAutoFreeze_ = false;
    } else
      return;
  } else if (targetIsSet) {
    targetObject.add(childValue);
  }
  if (isDraftable(childValue) && !isFrozen(childValue)) {
    if (!rootScope.immer_.autoFreeze_ && rootScope.unfinalizedDrafts_ < 1) {
      return;
    }
    finalize(rootScope, childValue);
    if ((!parentState || !parentState.scope_.parent_) && typeof prop !== "symbol" && Object.prototype.propertyIsEnumerable.call(targetObject, prop))
      maybeFreeze(rootScope, childValue);
  }
}
function maybeFreeze(scope, value, deep = false) {
  if (!scope.parent_ && scope.immer_.autoFreeze_ && scope.canAutoFreeze_) {
    freeze(value, deep);
  }
}

// src/core/proxy.ts
function createProxyProxy(base, parent) {
  const isArray = Array.isArray(base);
  const state = {
    type_: isArray ? 1 /* Array */ : 0 /* Object */,
    // Track which produce call this is associated with.
    scope_: parent ? parent.scope_ : getCurrentScope(),
    // True for both shallow and deep changes.
    modified_: false,
    // Used during finalization.
    finalized_: false,
    // Track which properties have been assigned (true) or deleted (false).
    assigned_: {},
    // The parent draft state.
    parent_: parent,
    // The base state.
    base_: base,
    // The base proxy.
    draft_: null,
    // set below
    // The base copy with any updated values.
    copy_: null,
    // Called by the `produce` function.
    revoke_: null,
    isManual_: false
  };
  let target = state;
  let traps = objectTraps;
  if (isArray) {
    target = [state];
    traps = arrayTraps;
  }
  const { revoke, proxy } = Proxy.revocable(target, traps);
  state.draft_ = proxy;
  state.revoke_ = revoke;
  return proxy;
}
var objectTraps = {
  get(state, prop) {
    if (prop === DRAFT_STATE)
      return state;
    const source = latest(state);
    if (!has(source, prop)) {
      return readPropFromProto(state, source, prop);
    }
    const value = source[prop];
    if (state.finalized_ || !isDraftable(value)) {
      return value;
    }
    if (value === peek(state.base_, prop)) {
      prepareCopy(state);
      return state.copy_[prop] = createProxy(value, state);
    }
    return value;
  },
  has(state, prop) {
    return prop in latest(state);
  },
  ownKeys(state) {
    return Reflect.ownKeys(latest(state));
  },
  set(state, prop, value) {
    const desc = getDescriptorFromProto(latest(state), prop);
    if (desc?.set) {
      desc.set.call(state.draft_, value);
      return true;
    }
    if (!state.modified_) {
      const current2 = peek(latest(state), prop);
      const currentState = current2?.[DRAFT_STATE];
      if (currentState && currentState.base_ === value) {
        state.copy_[prop] = value;
        state.assigned_[prop] = false;
        return true;
      }
      if (is(value, current2) && (value !== void 0 || has(state.base_, prop)))
        return true;
      prepareCopy(state);
      markChanged(state);
    }
    if (state.copy_[prop] === value && // special case: handle new props with value 'undefined'
    (value !== void 0 || prop in state.copy_) || // special case: NaN
    Number.isNaN(value) && Number.isNaN(state.copy_[prop]))
      return true;
    state.copy_[prop] = value;
    state.assigned_[prop] = true;
    return true;
  },
  deleteProperty(state, prop) {
    if (peek(state.base_, prop) !== void 0 || prop in state.base_) {
      state.assigned_[prop] = false;
      prepareCopy(state);
      markChanged(state);
    } else {
      delete state.assigned_[prop];
    }
    if (state.copy_) {
      delete state.copy_[prop];
    }
    return true;
  },
  // Note: We never coerce `desc.value` into an Immer draft, because we can't make
  // the same guarantee in ES5 mode.
  getOwnPropertyDescriptor(state, prop) {
    const owner = latest(state);
    const desc = Reflect.getOwnPropertyDescriptor(owner, prop);
    if (!desc)
      return desc;
    return {
      writable: true,
      configurable: state.type_ !== 1 /* Array */ || prop !== "length",
      enumerable: desc.enumerable,
      value: owner[prop]
    };
  },
  defineProperty() {
    die(11);
  },
  getPrototypeOf(state) {
    return getPrototypeOf(state.base_);
  },
  setPrototypeOf() {
    die(12);
  }
};
var arrayTraps = {};
each(objectTraps, (key, fn) => {
  arrayTraps[key] = function() {
    arguments[0] = arguments[0][0];
    return fn.apply(this, arguments);
  };
});
arrayTraps.deleteProperty = function(state, prop) {
  if ( true && isNaN(parseInt(prop)))
    die(13);
  return arrayTraps.set.call(this, state, prop, void 0);
};
arrayTraps.set = function(state, prop, value) {
  if ( true && prop !== "length" && isNaN(parseInt(prop)))
    die(14);
  return objectTraps.set.call(this, state[0], prop, value, state[0]);
};
function peek(draft, prop) {
  const state = draft[DRAFT_STATE];
  const source = state ? latest(state) : draft;
  return source[prop];
}
function readPropFromProto(state, source, prop) {
  const desc = getDescriptorFromProto(source, prop);
  return desc ? `value` in desc ? desc.value : (
    // This is a very special case, if the prop is a getter defined by the
    // prototype, we should invoke it with the draft as context!
    desc.get?.call(state.draft_)
  ) : void 0;
}
function getDescriptorFromProto(source, prop) {
  if (!(prop in source))
    return void 0;
  let proto = getPrototypeOf(source);
  while (proto) {
    const desc = Object.getOwnPropertyDescriptor(proto, prop);
    if (desc)
      return desc;
    proto = getPrototypeOf(proto);
  }
  return void 0;
}
function markChanged(state) {
  if (!state.modified_) {
    state.modified_ = true;
    if (state.parent_) {
      markChanged(state.parent_);
    }
  }
}
function prepareCopy(state) {
  if (!state.copy_) {
    state.copy_ = shallowCopy(
      state.base_,
      state.scope_.immer_.useStrictShallowCopy_
    );
  }
}

// src/core/immerClass.ts
var Immer2 = class {
  constructor(config) {
    this.autoFreeze_ = true;
    this.useStrictShallowCopy_ = false;
    /**
     * The `produce` function takes a value and a "recipe function" (whose
     * return value often depends on the base state). The recipe function is
     * free to mutate its first argument however it wants. All mutations are
     * only ever applied to a __copy__ of the base state.
     *
     * Pass only a function to create a "curried producer" which relieves you
     * from passing the recipe function every time.
     *
     * Only plain objects and arrays are made mutable. All other objects are
     * considered uncopyable.
     *
     * Note: This function is __bound__ to its `Immer` instance.
     *
     * @param {any} base - the initial state
     * @param {Function} recipe - function that receives a proxy of the base state as first argument and which can be freely modified
     * @param {Function} patchListener - optional function that will be called with all the patches produced here
     * @returns {any} a new state, or the initial state if nothing was modified
     */
    this.produce = (base, recipe, patchListener) => {
      if (typeof base === "function" && typeof recipe !== "function") {
        const defaultBase = recipe;
        recipe = base;
        const self = this;
        return function curriedProduce(base2 = defaultBase, ...args) {
          return self.produce(base2, (draft) => recipe.call(this, draft, ...args));
        };
      }
      if (typeof recipe !== "function")
        die(6);
      if (patchListener !== void 0 && typeof patchListener !== "function")
        die(7);
      let result;
      if (isDraftable(base)) {
        const scope = enterScope(this);
        const proxy = createProxy(base, void 0);
        let hasError = true;
        try {
          result = recipe(proxy);
          hasError = false;
        } finally {
          if (hasError)
            revokeScope(scope);
          else
            leaveScope(scope);
        }
        usePatchesInScope(scope, patchListener);
        return processResult(result, scope);
      } else if (!base || typeof base !== "object") {
        result = recipe(base);
        if (result === void 0)
          result = base;
        if (result === NOTHING)
          result = void 0;
        if (this.autoFreeze_)
          freeze(result, true);
        if (patchListener) {
          const p = [];
          const ip = [];
          getPlugin("Patches").generateReplacementPatches_(base, result, p, ip);
          patchListener(p, ip);
        }
        return result;
      } else
        die(1, base);
    };
    this.produceWithPatches = (base, recipe) => {
      if (typeof base === "function") {
        return (state, ...args) => this.produceWithPatches(state, (draft) => base(draft, ...args));
      }
      let patches, inversePatches;
      const result = this.produce(base, recipe, (p, ip) => {
        patches = p;
        inversePatches = ip;
      });
      return [result, patches, inversePatches];
    };
    if (typeof config?.autoFreeze === "boolean")
      this.setAutoFreeze(config.autoFreeze);
    if (typeof config?.useStrictShallowCopy === "boolean")
      this.setUseStrictShallowCopy(config.useStrictShallowCopy);
  }
  createDraft(base) {
    if (!isDraftable(base))
      die(8);
    if (isDraft(base))
      base = current(base);
    const scope = enterScope(this);
    const proxy = createProxy(base, void 0);
    proxy[DRAFT_STATE].isManual_ = true;
    leaveScope(scope);
    return proxy;
  }
  finishDraft(draft, patchListener) {
    const state = draft && draft[DRAFT_STATE];
    if (!state || !state.isManual_)
      die(9);
    const { scope_: scope } = state;
    usePatchesInScope(scope, patchListener);
    return processResult(void 0, scope);
  }
  /**
   * Pass true to automatically freeze all copies created by Immer.
   *
   * By default, auto-freezing is enabled.
   */
  setAutoFreeze(value) {
    this.autoFreeze_ = value;
  }
  /**
   * Pass true to enable strict shallow copy.
   *
   * By default, immer does not copy the object descriptors such as getter, setter and non-enumrable properties.
   */
  setUseStrictShallowCopy(value) {
    this.useStrictShallowCopy_ = value;
  }
  applyPatches(base, patches) {
    let i;
    for (i = patches.length - 1; i >= 0; i--) {
      const patch = patches[i];
      if (patch.path.length === 0 && patch.op === "replace") {
        base = patch.value;
        break;
      }
    }
    if (i > -1) {
      patches = patches.slice(i + 1);
    }
    const applyPatchesImpl = getPlugin("Patches").applyPatches_;
    if (isDraft(base)) {
      return applyPatchesImpl(base, patches);
    }
    return this.produce(
      base,
      (draft) => applyPatchesImpl(draft, patches)
    );
  }
};
function createProxy(value, parent) {
  const draft = isMap(value) ? getPlugin("MapSet").proxyMap_(value, parent) : isSet(value) ? getPlugin("MapSet").proxySet_(value, parent) : createProxyProxy(value, parent);
  const scope = parent ? parent.scope_ : getCurrentScope();
  scope.drafts_.push(draft);
  return draft;
}

// src/core/current.ts
function current(value) {
  if (!isDraft(value))
    die(10, value);
  return currentImpl(value);
}
function currentImpl(value) {
  if (!isDraftable(value) || isFrozen(value))
    return value;
  const state = value[DRAFT_STATE];
  let copy;
  if (state) {
    if (!state.modified_)
      return state.base_;
    state.finalized_ = true;
    copy = shallowCopy(value, state.scope_.immer_.useStrictShallowCopy_);
  } else {
    copy = shallowCopy(value, true);
  }
  each(copy, (key, childValue) => {
    set(copy, key, currentImpl(childValue));
  });
  if (state) {
    state.finalized_ = false;
  }
  return copy;
}

// src/plugins/patches.ts
function enablePatches() {
  const errorOffset = 16;
  if (true) {
    errors.push(
      'Sets cannot have "replace" patches.',
      function(op) {
        return "Unsupported patch operation: " + op;
      },
      function(path) {
        return "Cannot apply patch, path doesn't resolve: " + path;
      },
      "Patching reserved attributes like __proto__, prototype and constructor is not allowed"
    );
  }
  const REPLACE = "replace";
  const ADD = "add";
  const REMOVE = "remove";
  function generatePatches_(state, basePath, patches, inversePatches) {
    switch (state.type_) {
      case 0 /* Object */:
      case 2 /* Map */:
        return generatePatchesFromAssigned(
          state,
          basePath,
          patches,
          inversePatches
        );
      case 1 /* Array */:
        return generateArrayPatches(state, basePath, patches, inversePatches);
      case 3 /* Set */:
        return generateSetPatches(
          state,
          basePath,
          patches,
          inversePatches
        );
    }
  }
  function generateArrayPatches(state, basePath, patches, inversePatches) {
    let { base_, assigned_ } = state;
    let copy_ = state.copy_;
    if (copy_.length < base_.length) {
      ;
      [base_, copy_] = [copy_, base_];
      [patches, inversePatches] = [inversePatches, patches];
    }
    for (let i = 0; i < base_.length; i++) {
      if (assigned_[i] && copy_[i] !== base_[i]) {
        const path = basePath.concat([i]);
        patches.push({
          op: REPLACE,
          path,
          // Need to maybe clone it, as it can in fact be the original value
          // due to the base/copy inversion at the start of this function
          value: clonePatchValueIfNeeded(copy_[i])
        });
        inversePatches.push({
          op: REPLACE,
          path,
          value: clonePatchValueIfNeeded(base_[i])
        });
      }
    }
    for (let i = base_.length; i < copy_.length; i++) {
      const path = basePath.concat([i]);
      patches.push({
        op: ADD,
        path,
        // Need to maybe clone it, as it can in fact be the original value
        // due to the base/copy inversion at the start of this function
        value: clonePatchValueIfNeeded(copy_[i])
      });
    }
    for (let i = copy_.length - 1; base_.length <= i; --i) {
      const path = basePath.concat([i]);
      inversePatches.push({
        op: REMOVE,
        path
      });
    }
  }
  function generatePatchesFromAssigned(state, basePath, patches, inversePatches) {
    const { base_, copy_ } = state;
    each(state.assigned_, (key, assignedValue) => {
      const origValue = get(base_, key);
      const value = get(copy_, key);
      const op = !assignedValue ? REMOVE : has(base_, key) ? REPLACE : ADD;
      if (origValue === value && op === REPLACE)
        return;
      const path = basePath.concat(key);
      patches.push(op === REMOVE ? { op, path } : { op, path, value });
      inversePatches.push(
        op === ADD ? { op: REMOVE, path } : op === REMOVE ? { op: ADD, path, value: clonePatchValueIfNeeded(origValue) } : { op: REPLACE, path, value: clonePatchValueIfNeeded(origValue) }
      );
    });
  }
  function generateSetPatches(state, basePath, patches, inversePatches) {
    let { base_, copy_ } = state;
    let i = 0;
    base_.forEach((value) => {
      if (!copy_.has(value)) {
        const path = basePath.concat([i]);
        patches.push({
          op: REMOVE,
          path,
          value
        });
        inversePatches.unshift({
          op: ADD,
          path,
          value
        });
      }
      i++;
    });
    i = 0;
    copy_.forEach((value) => {
      if (!base_.has(value)) {
        const path = basePath.concat([i]);
        patches.push({
          op: ADD,
          path,
          value
        });
        inversePatches.unshift({
          op: REMOVE,
          path,
          value
        });
      }
      i++;
    });
  }
  function generateReplacementPatches_(baseValue, replacement, patches, inversePatches) {
    patches.push({
      op: REPLACE,
      path: [],
      value: replacement === NOTHING ? void 0 : replacement
    });
    inversePatches.push({
      op: REPLACE,
      path: [],
      value: baseValue
    });
  }
  function applyPatches_(draft, patches) {
    patches.forEach((patch) => {
      const { path, op } = patch;
      let base = draft;
      for (let i = 0; i < path.length - 1; i++) {
        const parentType = getArchtype(base);
        let p = path[i];
        if (typeof p !== "string" && typeof p !== "number") {
          p = "" + p;
        }
        if ((parentType === 0 /* Object */ || parentType === 1 /* Array */) && (p === "__proto__" || p === "constructor"))
          die(errorOffset + 3);
        if (typeof base === "function" && p === "prototype")
          die(errorOffset + 3);
        base = get(base, p);
        if (typeof base !== "object")
          die(errorOffset + 2, path.join("/"));
      }
      const type = getArchtype(base);
      const value = deepClonePatchValue(patch.value);
      const key = path[path.length - 1];
      switch (op) {
        case REPLACE:
          switch (type) {
            case 2 /* Map */:
              return base.set(key, value);
            case 3 /* Set */:
              die(errorOffset);
            default:
              return base[key] = value;
          }
        case ADD:
          switch (type) {
            case 1 /* Array */:
              return key === "-" ? base.push(value) : base.splice(key, 0, value);
            case 2 /* Map */:
              return base.set(key, value);
            case 3 /* Set */:
              return base.add(value);
            default:
              return base[key] = value;
          }
        case REMOVE:
          switch (type) {
            case 1 /* Array */:
              return base.splice(key, 1);
            case 2 /* Map */:
              return base.delete(key);
            case 3 /* Set */:
              return base.delete(patch.value);
            default:
              return delete base[key];
          }
        default:
          die(errorOffset + 1, op);
      }
    });
    return draft;
  }
  function deepClonePatchValue(obj) {
    if (!isDraftable(obj))
      return obj;
    if (Array.isArray(obj))
      return obj.map(deepClonePatchValue);
    if (isMap(obj))
      return new Map(
        Array.from(obj.entries()).map(([k, v]) => [k, deepClonePatchValue(v)])
      );
    if (isSet(obj))
      return new Set(Array.from(obj).map(deepClonePatchValue));
    const cloned = Object.create(getPrototypeOf(obj));
    for (const key in obj)
      cloned[key] = deepClonePatchValue(obj[key]);
    if (has(obj, DRAFTABLE))
      cloned[DRAFTABLE] = obj[DRAFTABLE];
    return cloned;
  }
  function clonePatchValueIfNeeded(obj) {
    if (isDraft(obj)) {
      return deepClonePatchValue(obj);
    } else
      return obj;
  }
  loadPlugin("Patches", {
    applyPatches_,
    generatePatches_,
    generateReplacementPatches_
  });
}

// src/plugins/mapset.ts
function enableMapSet() {
  class DraftMap extends Map {
    constructor(target, parent) {
      super();
      this[DRAFT_STATE] = {
        type_: 2 /* Map */,
        parent_: parent,
        scope_: parent ? parent.scope_ : getCurrentScope(),
        modified_: false,
        finalized_: false,
        copy_: void 0,
        assigned_: void 0,
        base_: target,
        draft_: this,
        isManual_: false,
        revoked_: false
      };
    }
    get size() {
      return latest(this[DRAFT_STATE]).size;
    }
    has(key) {
      return latest(this[DRAFT_STATE]).has(key);
    }
    set(key, value) {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (!latest(state).has(key) || latest(state).get(key) !== value) {
        prepareMapCopy(state);
        markChanged(state);
        state.assigned_.set(key, true);
        state.copy_.set(key, value);
        state.assigned_.set(key, true);
      }
      return this;
    }
    delete(key) {
      if (!this.has(key)) {
        return false;
      }
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      prepareMapCopy(state);
      markChanged(state);
      if (state.base_.has(key)) {
        state.assigned_.set(key, false);
      } else {
        state.assigned_.delete(key);
      }
      state.copy_.delete(key);
      return true;
    }
    clear() {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (latest(state).size) {
        prepareMapCopy(state);
        markChanged(state);
        state.assigned_ = /* @__PURE__ */ new Map();
        each(state.base_, (key) => {
          state.assigned_.set(key, false);
        });
        state.copy_.clear();
      }
    }
    forEach(cb, thisArg) {
      const state = this[DRAFT_STATE];
      latest(state).forEach((_value, key, _map) => {
        cb.call(thisArg, this.get(key), key, this);
      });
    }
    get(key) {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      const value = latest(state).get(key);
      if (state.finalized_ || !isDraftable(value)) {
        return value;
      }
      if (value !== state.base_.get(key)) {
        return value;
      }
      const draft = createProxy(value, state);
      prepareMapCopy(state);
      state.copy_.set(key, draft);
      return draft;
    }
    keys() {
      return latest(this[DRAFT_STATE]).keys();
    }
    values() {
      const iterator = this.keys();
      return {
        [Symbol.iterator]: () => this.values(),
        next: () => {
          const r = iterator.next();
          if (r.done)
            return r;
          const value = this.get(r.value);
          return {
            done: false,
            value
          };
        }
      };
    }
    entries() {
      const iterator = this.keys();
      return {
        [Symbol.iterator]: () => this.entries(),
        next: () => {
          const r = iterator.next();
          if (r.done)
            return r;
          const value = this.get(r.value);
          return {
            done: false,
            value: [r.value, value]
          };
        }
      };
    }
    [(DRAFT_STATE, Symbol.iterator)]() {
      return this.entries();
    }
  }
  function proxyMap_(target, parent) {
    return new DraftMap(target, parent);
  }
  function prepareMapCopy(state) {
    if (!state.copy_) {
      state.assigned_ = /* @__PURE__ */ new Map();
      state.copy_ = new Map(state.base_);
    }
  }
  class DraftSet extends Set {
    constructor(target, parent) {
      super();
      this[DRAFT_STATE] = {
        type_: 3 /* Set */,
        parent_: parent,
        scope_: parent ? parent.scope_ : getCurrentScope(),
        modified_: false,
        finalized_: false,
        copy_: void 0,
        base_: target,
        draft_: this,
        drafts_: /* @__PURE__ */ new Map(),
        revoked_: false,
        isManual_: false
      };
    }
    get size() {
      return latest(this[DRAFT_STATE]).size;
    }
    has(value) {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (!state.copy_) {
        return state.base_.has(value);
      }
      if (state.copy_.has(value))
        return true;
      if (state.drafts_.has(value) && state.copy_.has(state.drafts_.get(value)))
        return true;
      return false;
    }
    add(value) {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (!this.has(value)) {
        prepareSetCopy(state);
        markChanged(state);
        state.copy_.add(value);
      }
      return this;
    }
    delete(value) {
      if (!this.has(value)) {
        return false;
      }
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      prepareSetCopy(state);
      markChanged(state);
      return state.copy_.delete(value) || (state.drafts_.has(value) ? state.copy_.delete(state.drafts_.get(value)) : (
        /* istanbul ignore next */
        false
      ));
    }
    clear() {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (latest(state).size) {
        prepareSetCopy(state);
        markChanged(state);
        state.copy_.clear();
      }
    }
    values() {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      prepareSetCopy(state);
      return state.copy_.values();
    }
    entries() {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      prepareSetCopy(state);
      return state.copy_.entries();
    }
    keys() {
      return this.values();
    }
    [(DRAFT_STATE, Symbol.iterator)]() {
      return this.values();
    }
    forEach(cb, thisArg) {
      const iterator = this.values();
      let result = iterator.next();
      while (!result.done) {
        cb.call(thisArg, result.value, result.value, this);
        result = iterator.next();
      }
    }
  }
  function proxySet_(target, parent) {
    return new DraftSet(target, parent);
  }
  function prepareSetCopy(state) {
    if (!state.copy_) {
      state.copy_ = /* @__PURE__ */ new Set();
      state.base_.forEach((value) => {
        if (isDraftable(value)) {
          const draft = createProxy(value, state);
          state.drafts_.set(value, draft);
          state.copy_.add(draft);
        } else {
          state.copy_.add(value);
        }
      });
    }
  }
  function assertUnrevoked(state) {
    if (state.revoked_)
      die(3, JSON.stringify(latest(state)));
  }
  loadPlugin("MapSet", { proxyMap_, proxySet_ });
}

// src/immer.ts
var immer = new Immer2();
var produce = immer.produce;
var produceWithPatches = immer.produceWithPatches.bind(
  immer
);
var setAutoFreeze = immer.setAutoFreeze.bind(immer);
var setUseStrictShallowCopy = immer.setUseStrictShallowCopy.bind(immer);
var applyPatches = immer.applyPatches.bind(immer);
var createDraft = immer.createDraft.bind(immer);
var finishDraft = immer.finishDraft.bind(immer);
function castDraft(value) {
  return value;
}
function castImmutable(value) {
  return value;
}

//# sourceMappingURL=immer.mjs.map

/***/ }),

/***/ "../bpl-tools/utils/common.js":
/*!************************************!*\
  !*** ../bpl-tools/utils/common.js ***!
  \************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   SVGSanitizer: () => (/* binding */ SVGSanitizer),
/* harmony export */   escapeHTML: () => (/* binding */ escapeHTML),
/* harmony export */   isExist: () => (/* binding */ isExist),
/* harmony export */   sanitizeHTML: () => (/* binding */ sanitizeHTML),
/* harmony export */   sanitizeInput: () => (/* binding */ sanitizeInput),
/* harmony export */   sanitizeURL: () => (/* binding */ sanitizeURL)
/* harmony export */ });
const isExist = value => {
  if (value === undefined || value === null || value === '') {
    return false;
  }
  if (Array.isArray(value) && value.length === 0) {
    return false;
  }
  if (typeof value === 'object' && Object.keys(value).length === 0) {
    return false;
  }
  if (typeof value === 'string' && value.trim() === '') {
    return false;
  }
  if (typeof value === 'number' && value === 0) {
    return false;
  }
  return true;
};
const escapeHTML = (input = '') => {
  if (!input) {
    return '';
  }

  // Regular expression to match all HTML tags and their attributes
  return input?.replace(/<([a-z][a-z0-9]*)\b([^>]*)>/gi, (match, tagName, attrs) => {
    // List of allowed tags and their attributes
    const allowedTags = ['b', 'strong', 'i', 'em', 'span', 'a', 'br'];
    const allowedAttrs = ['style', 'href', 'target', 'rel', 'class'];

    // If the tag is allowed, keep it, but sanitize its attributes
    if (allowedTags.includes(tagName.toLowerCase())) {
      // Process the tag's attributes
      const sanitizedAttrs = attrs.replace(/([a-z0-9-]+)=["'][^"']*["']/gi, (attrMatch, attrName) => {
        // Only keep allowed attributes
        if (allowedAttrs.includes(attrName.toLowerCase())) {
          return attrMatch; // Keep allowed attributes as they are
        }
        return ''; // Remove any other attributes
      });
      return `<${tagName}${sanitizedAttrs}>`;
    }
    return match?.replace(/</g, '&lt;').replace(/>/g, '&gt;');
  });
};
const sanitizeURL = inputUrl => {
  try {
    const url = new URL(inputUrl);

    // 1. Check for safe protocols
    if (!['http:', 'https:'].includes(url.protocol)) {
      return null;
    } else {
      // 2. Strip query and fragment for safety
      // url.search = '';
      // url.hash = '';

      return url.toString();
    }
  } catch (err) {
    if (typeof inputUrl === 'string' && inputUrl.startsWith('/') && !inputUrl.startsWith('//')) {
      return inputUrl;
    } else {
      return null;
    }
  }
};
const sanitizeHTML = input => {
  const parser = new DOMParser();
  const doc = parser.parseFromString(input, 'text/html');
  const allowedTags = ['b', 'strong', 'i', 'em', 'span', 'a', 'br'];
  const allowedAttrs = ['style', 'href', 'target', 'rel', 'class'];
  doc.body.querySelectorAll('*').forEach(node => {
    // Remove disallowed tags
    if (!allowedTags.includes(node.tagName.toLowerCase())) {
      node.remove();
      return;
    }

    // Loop through attributes and sanitize
    [...node.attributes].forEach(attr => {
      if (!allowedAttrs.includes(attr.name)) {
        node.removeAttribute(attr.name);
      }

      // if (attr.name === 'href' && attr.value.trim().toLowerCase().startsWith('javascript:')) {
      // 	node.removeAttribute('href');
      // }

      if (attr.name === 'href') {
        const sanitizeHref = sanitizeURL(attr.value);
        if (sanitizeHref) {
          node.setAttribute('href', sanitizeHref);
        } else {
          node.removeAttribute('href');
        }
      }
    });
  });
  return doc.body.innerHTML;
};
const sanitizeInput = input => {
  return input.replace(/[<>]/g, '').replace(/javascript:/gi, '').replace(/on\w+=/gi, '').trim();
};

//-------- Sanitize SVG ----------//
class SVGSanitizer {
  constructor(options = {}) {
    this.defaultOptions = {
      allowedTags: ['svg', 'g', 'path', 'rect', 'circle', 'ellipse', 'line', 'polyline', 'polygon', 'text', 'tspan', 'defs', 'clipPath', 'mask', 'linearGradient', 'radialGradient', 'stop', 'style', 'title', 'desc'],
      allowedAttributes: ['id', 'class', 'style', 'transform', 'd', 'x', 'y', 'width', 'height', 'cx', 'cy', 'r', 'rx', 'ry', 'points', 'x1', 'y1', 'x2', 'y2', 'fill', 'stroke', 'stroke-width', 'stroke-linecap', 'stroke-linejoin', 'opacity', 'fill-opacity', 'stroke-opacity', 'viewBox', 'preserveAspectRatio', 'xmlns', 'xmlns:xlink'],
      allowedProtocols: ['http', 'https', 'data'],
      removeScripts: true,
      removeEvents: true,
      removeExternalResources: true,
      sanitizeStyle: true
    };
    this.options = {
      ...this.defaultOptions,
      ...options
    };
  }
  sanitize(svgString) {
    const parser = new DOMParser();
    const doc = parser.parseFromString(svgString, 'image/svg+xml');
    this.removeScripts(doc);
    this.sanitizeElements(doc);
    const serializer = new XMLSerializer();
    return serializer.serializeToString(doc.documentElement);
  }

  // ✅ New method: sanitize from File object
  async sanitizeFile(file) {
    const response = await fetch(file);
    const svgText = await response.text();
    // console.log(svgText)
    return this.sanitize(`${svgText}`);
    // return "";
  }
  removeScripts(doc) {
    if (this.options.removeScripts) {
      const scripts = doc.querySelectorAll('script');
      scripts.forEach(script => script.remove());
      const allElements = doc.querySelectorAll('*');
      allElements.forEach(el => {
        if (el.tagName.toLowerCase().includes('script')) {
          el.remove();
        }
      });
    }
  }
  sanitizeElements(doc) {
    const allElements = doc.querySelectorAll('*');
    allElements.forEach(element => {
      const tagName = element.tagName.toLowerCase();
      if (!this.options.allowedTags.includes(tagName)) {
        element.remove();
        return;
      }
      this.sanitizeAttributes(element);
    });
  }
  sanitizeAttributes(element) {
    const attributes = Array.from(element.attributes);
    attributes.forEach(attr => {
      const attrName = attr.name.toLowerCase();
      const attrValue = attr.value;
      if (this.options.removeEvents && attrName.startsWith('on')) {
        element.removeAttribute(attrName);
        return;
      }
      if (this.options.removeExternalResources) {
        if ((attrName === 'href' || attrName === 'xlink:href') && !this.isAllowedUrl(attrValue)) {
          element.removeAttribute(attrName);
          return;
        }
      }
      const baseAttrName = attrName.replace('xlink:', '');
      if (!this.options.allowedAttributes.includes(baseAttrName)) {
        element.removeAttribute(attrName);
        return;
      }
      if (attrName === 'style' && this.options.sanitizeStyle) {
        this.sanitizeStyleAttribute(element, attrValue);
      }
    });
  }
  isAllowedUrl(url) {
    if (url.startsWith('data:') || url.startsWith('#')) {
      return true;
    }
    try {
      const parsedUrl = new URL(url);
      return this.options.allowedProtocols.includes(parsedUrl.protocol.replace(':', ''));
    } catch {
      return false;
    }
  }
  sanitizeStyleAttribute(element, styleValue) {
    const safeStyle = styleValue.replace(/expression\(|javascript:|url\(javascript:/gi, '').replace(/behavior\s*:/gi, '').replace(/binding\s*:/gi, '');
    element.setAttribute('style', safeStyle);
  }
}

/***/ }),

/***/ "../bpl-tools/utils/data.js":
/*!**********************************!*\
  !*** ../bpl-tools/utils/data.js ***!
  \**********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   contentColor: () => (/* binding */ contentColor),
/* harmony export */   deskBreakpoint: () => (/* binding */ deskBreakpoint),
/* harmony export */   gradient: () => (/* binding */ gradient),
/* harmony export */   mobileBreakpoint: () => (/* binding */ mobileBreakpoint),
/* harmony export */   primaryColor: () => (/* binding */ primaryColor),
/* harmony export */   primaryColor100: () => (/* binding */ primaryColor100),
/* harmony export */   primaryColor1000: () => (/* binding */ primaryColor1000),
/* harmony export */   primaryColor200: () => (/* binding */ primaryColor200),
/* harmony export */   primaryColor300: () => (/* binding */ primaryColor300),
/* harmony export */   primaryColor400: () => (/* binding */ primaryColor400),
/* harmony export */   primaryColor500: () => (/* binding */ primaryColor500),
/* harmony export */   primaryColor600: () => (/* binding */ primaryColor600),
/* harmony export */   primaryColor700: () => (/* binding */ primaryColor700),
/* harmony export */   primaryColor800: () => (/* binding */ primaryColor800),
/* harmony export */   primaryColor900: () => (/* binding */ primaryColor900),
/* harmony export */   secondaryColor: () => (/* binding */ secondaryColor),
/* harmony export */   tabBreakpoint: () => (/* binding */ tabBreakpoint),
/* harmony export */   titleColor: () => (/* binding */ titleColor)
/* harmony export */ });
const deskBreakpoint = '@media only screen and (min-width: 1025px)';
const tabBreakpoint = '@media only screen and (max-width: 1024px)';
const mobileBreakpoint = '@media only screen and (max-width: 640px)';
const primaryColor = '#146EF5';
const primaryColor100 = '#e7f0fe';
const primaryColor200 = '#b6d2fc';
const primaryColor300 = '#85b4fa';
const primaryColor400 = '#5495f8';
const primaryColor500 = '#2377f6';
const primaryColor600 = '#095edc';
const primaryColor700 = '#0749ab';
const primaryColor800 = '#05347a';
const primaryColor900 = '#031f49';
const primaryColor1000 = '#010a18';
const secondaryColor = '#FF7A00';
const titleColor = '#070127';
const contentColor = '#485781';
const gradient = 'linear-gradient(135deg, #0040E3, #18D4FD)';

/***/ }),

/***/ "../bpl-tools/utils/functions.js":
/*!***************************************!*\
  !*** ../bpl-tools/utils/functions.js ***!
  \***************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   MimeTypeChecker: () => (/* binding */ MimeTypeChecker),
/* harmony export */   debounce: () => (/* binding */ debounce),
/* harmony export */   getBoxValue: () => (/* binding */ getBoxValue),
/* harmony export */   getImageSizes: () => (/* binding */ getImageSizes),
/* harmony export */   getRandomColor: () => (/* binding */ getRandomColor),
/* harmony export */   randomNumber: () => (/* binding */ randomNumber),
/* harmony export */   tabController: () => (/* binding */ tabController),
/* harmony export */   updateAttributes: () => (/* binding */ updateAttributes),
/* harmony export */   updateData: () => (/* binding */ updateData)
/* harmony export */ });
/* harmony import */ var immer__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! immer */ "../bpl-tools/node_modules/immer/dist/immer.mjs");

const getBoxValue = object => Object.values(object).join(' ');
const getImageSizes = (image, imageSizes) => {
  if (!image) return [];
  let options = [];
  const sizes = image.media_details.sizes;
  for (const key in sizes) {
    const imageSize = imageSizes.find(s => s.slug === key);
    if (imageSize) {
      options.push({
        label: imageSize.name,
        value: sizes[key].source_url
      });
    }
  }
  return options;
};
const tabController = () => {
  setTimeout(() => {
    const panelBodies = document.querySelectorAll('.bPlPanelBody:not(.itemPanelBody) > h2 > button');
    panelBodies.forEach(item => {
      item.addEventListener('click', clickEveryItem);
    });
    function clickEveryItem() {
      this.removeEventListener('click', clickEveryItem);
      panelBodies.forEach(item => {
        if (item.getAttribute('aria-expanded') === 'true' && !item.isEqualNode(this)) {
          item.click();
        }
      });
      setTimeout(() => {
        this.addEventListener('click', clickEveryItem);
      }, 500);
    }
  }, 500);
};
const updateData = (attr, value, ...props) => {
  if (props.length === 0) {
    return value;
  }
  const [currentProp, ...remainingProps] = props;
  if (remainingProps.length === 0) {
    return (0,immer__WEBPACK_IMPORTED_MODULE_0__.produce)(attr, draft => {
      if (Array.isArray(draft[currentProp]) && (draft === null || draft === undefined)) {
        draft = {};
      }
      draft[currentProp] = value;
    });
  }
  return (0,immer__WEBPACK_IMPORTED_MODULE_0__.produce)(attr, draft => {
    if (draft === null || draft === undefined) {
      draft = {};
    }
    if (!Object.prototype.hasOwnProperty.call(draft, currentProp)) {
      draft[currentProp] = {};
    }
    draft[currentProp] = updateData(draft[currentProp], value, ...remainingProps);
  });
};
const updateAttributes = (attributes, setAttributes) => {
  return (object, val, ...props) => {
    setAttributes({
      [object]: updateData(attributes[object], val, ...props)
    });
  };
};
const debounce = (fn, delay) => {
  let timeoutId;
  return (...args) => {
    clearTimeout(timeoutId);
    timeoutId = setTimeout(() => {
      fn(...args);
    }, delay);
  };
};
const getRandomColor = () => {
  return '#' + Math.floor(Math.random() * 16777215).toString(16).padStart(6, '0');
};
const randomNumber = number => parseInt(Math.random() * number);
class MimeTypeChecker {
  // Common MIME types mapping

  static get mimeTypes() {
    return {
      // Images
      'jpg': 'image/jpeg',
      'jpeg': 'image/jpeg',
      'png': 'image/png',
      'gif': 'image/gif',
      'webp': 'image/webp',
      'svg': 'image/svg+xml',
      'bmp': 'image/bmp',
      'ico': 'image/x-icon',
      // Documents
      'pdf': 'application/pdf',
      'doc': 'application/msword',
      'docx': 'application/vnd.openxmlformats-officedocument.wordprocessingml.document',
      'xls': 'application/vnd.ms-excel',
      'xlsx': 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
      'ppt': 'application/vnd.ms-powerpoint',
      'pptx': 'application/vnd.openxmlformats-officedocument.presentationml.presentation',
      'txt': 'text/plain',
      'csv': 'text/csv',
      // Audio/Video
      'mp3': 'audio/mpeg',
      'wav': 'audio/wav',
      'ogg': 'audio/ogg',
      'mp4': 'video/mp4',
      'webm': 'video/webm',
      'avi': 'video/x-msvideo',
      'mov': 'video/quicktime',
      // Archives
      'zip': 'application/zip',
      'rar': 'application/x-rar-compressed',
      'tar': 'application/x-tar',
      'gz': 'application/gzip',
      // Code
      'js': 'application/javascript',
      'css': 'text/css',
      'html': 'text/html',
      'json': 'application/json',
      'xml': 'application/xml'
    };
  }

  // Check if a file has a valid MIME type (now handles both string and array)
  static isValidMimeType(file, allowedTypes = []) {
    if (!file) return false;
    const fileType = this.getMimeTypeFromFile(file);
    if (!fileType) return false;

    // Convert single string to array for consistent processing
    const typesArray = Array.isArray(allowedTypes) ? allowedTypes : [allowedTypes];
    if (typesArray.length === 0) return true;
    return typesArray.some(allowedType => {
      if (allowedType.includes('*')) {
        // Handle wildcard types like 'image/*'
        const baseType = allowedType.split('/*')[0];
        return fileType.startsWith(baseType);
      }
      return fileType === allowedType;
    });
  }

  // Get MIME type from file extension
  static getMimeTypeFromExtension(filename) {
    if (!filename) return null;
    const extension = filename.split('.').pop().toLowerCase();
    return this.mimeTypes[extension] || null;
  }

  // Get MIME type from File object
  static getMimeTypeFromFile(file) {
    if (file instanceof File || file instanceof Blob) {
      return file.type || this.getMimeTypeFromExtension(file.name);
    }
    return this.getMimeTypeFromExtension(file);
  }

  // Check if file is an image
  static isImage(file) {
    const mimeType = this.getMimeTypeFromFile(file);
    return mimeType ? mimeType.startsWith('image/') : false;
  }

  // Check if file is a video
  static isVideo(file) {
    const mimeType = this.getMimeTypeFromFile(file);
    return mimeType ? mimeType.startsWith('video/') : false;
  }

  // Check if file is an audio file
  static isAudio(file) {
    const mimeType = this.getMimeTypeFromFile(file);
    return mimeType ? mimeType.startsWith('audio/') : false;
  }

  // Check if file is a document
  static isDocument(file) {
    const mimeType = this.getMimeTypeFromFile(file);
    return mimeType ? mimeType.startsWith('application/') || mimeType.startsWith('text/') || mimeType.includes('word') || mimeType.includes('excel') || mimeType.includes('powerpoint') || mimeType.includes('pdf') : false;
  }

  // Validate file against specific MIME types
  // Validate file against specific MIME types (updated to handle string/array)
  static validateFile(file, options = {}) {
    const {
      allowedTypes = [],
      maxSize = Infinity,
      minSize = 0
    } = options;
    const errors = [];

    // Convert allowedTypes to array if it's a string
    const typesArray = Array.isArray(allowedTypes) ? allowedTypes : [allowedTypes];

    // Check MIME type
    if (typesArray.length > 0 && !this.isValidMimeType(file, typesArray)) {
      const allowedTypesString = typesArray.join(', ');
      errors.push(`File type not allowed. Allowed types: ${allowedTypesString}`);
    }

    // Check file size (only if it's a File/Blob object)
    if (file instanceof File || file instanceof Blob) {
      if (file.size > maxSize) {
        errors.push(`File size too large. Maximum size: ${this.formatBytes(maxSize)}`);
      }
      if (file.size < minSize) {
        errors.push(`File size too small. Minimum size: ${this.formatBytes(minSize)}`);
      }
    }
    return {
      isValid: errors.length === 0,
      errors,
      mimeType: this.getMimeTypeFromFile(file),
      size: file instanceof File || file instanceof Blob ? file.size : null
    };
  }

  // Helper to format bytes
  static formatBytes(bytes, decimals = 2) {
    if (bytes === 0) return '0 Bytes';
    const k = 1024;
    const dm = decimals < 0 ? 0 : decimals;
    const sizes = ['Bytes', 'KB', 'MB', 'GB', 'TB'];
    const i = Math.floor(Math.log(bytes) / Math.log(k));
    return parseFloat((bytes / Math.pow(k, i)).toFixed(dm)) + ' ' + sizes[i];
  }
  static addMimeType(extension, mimeType) {
    const types = this.mimeTypes;
    types[extension.toLowerCase()] = mimeType;
  }

  // Add an instance property that references the static methods
  static get helpers() {
    return {
      validateFile: this.validateFile.bind(this),
      isValidMimeType: this.isValidMimeType.bind(this),
      isImage: this.isImage.bind(this),
      isVideo: this.isVideo.bind(this),
      isAudio: this.isAudio.bind(this),
      isDocument: this.isDocument.bind(this),
      getMimeTypeFromFile: this.getMimeTypeFromFile.bind(this),
      getMimeTypeFromExtension: this.getMimeTypeFromExtension.bind(this),
      addMimeType: this.addMimeType.bind(this)
    };
  }
}

/***/ }),

/***/ "../bpl-tools/utils/getCSS.js":
/*!************************************!*\
  !*** ../bpl-tools/utils/getCSS.js ***!
  \************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   getAdvBGCSS: () => (/* binding */ getAdvBGCSS),
/* harmony export */   getBackgroundCSS: () => (/* binding */ getBackgroundCSS),
/* harmony export */   getBorderBoxCSS: () => (/* binding */ getBorderBoxCSS),
/* harmony export */   getBorderCSS: () => (/* binding */ getBorderCSS),
/* harmony export */   getBoxCSS: () => (/* binding */ getBoxCSS),
/* harmony export */   getColorsCSS: () => (/* binding */ getColorsCSS),
/* harmony export */   getGradientCSS: () => (/* binding */ getGradientCSS),
/* harmony export */   getIconCSS: () => (/* binding */ getIconCSS),
/* harmony export */   getMaskCSS: () => (/* binding */ getMaskCSS),
/* harmony export */   getMultiShadowCSS: () => (/* binding */ getMultiShadowCSS),
/* harmony export */   getOverlayCSS: () => (/* binding */ getOverlayCSS),
/* harmony export */   getSeparatorCSS: () => (/* binding */ getSeparatorCSS),
/* harmony export */   getShadowCSS: () => (/* binding */ getShadowCSS),
/* harmony export */   getSpaceCSS: () => (/* binding */ getSpaceCSS),
/* harmony export */   getTransformCSS: () => (/* binding */ getTransformCSS),
/* harmony export */   getTypoCSS: () => (/* binding */ getTypoCSS),
/* harmony export */   isValidCSS: () => (/* binding */ isValidCSS)
/* harmony export */ });
/* harmony import */ var _Components_Mask_assets_shapes_blob_svg__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../Components/Mask/assets/shapes/blob.svg */ "../bpl-tools/Components/Mask/assets/shapes/blob.svg");
/* harmony import */ var _Components_Mask_assets_shapes_circle_svg__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../Components/Mask/assets/shapes/circle.svg */ "../bpl-tools/Components/Mask/assets/shapes/circle.svg");
/* harmony import */ var _Components_Mask_assets_shapes_flower_svg__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../Components/Mask/assets/shapes/flower.svg */ "../bpl-tools/Components/Mask/assets/shapes/flower.svg");
/* harmony import */ var _Components_Mask_assets_shapes_hexagon_svg__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../Components/Mask/assets/shapes/hexagon.svg */ "../bpl-tools/Components/Mask/assets/shapes/hexagon.svg");
/* harmony import */ var _Components_Mask_assets_shapes_sketch_svg__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../Components/Mask/assets/shapes/sketch.svg */ "../bpl-tools/Components/Mask/assets/shapes/sketch.svg");
/* harmony import */ var _Components_Mask_assets_shapes_triangle_svg__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../Components/Mask/assets/shapes/triangle.svg */ "../bpl-tools/Components/Mask/assets/shapes/triangle.svg");
/* harmony import */ var _common__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./common */ "../bpl-tools/utils/common.js");
/* harmony import */ var _data__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./data */ "../bpl-tools/utils/data.js");








const isValidCSS = (p, v) => (0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(v) ? `${p}: ${v};` : '';
const getBackgroundCSS = (bg, isSolid = true, isGradient = true, isImage = true) => {
  const {
    type = 'solid',
    color = '',
    gradient = _data__WEBPACK_IMPORTED_MODULE_7__.gradient,
    image = {},
    position = 'center center',
    attachment = '',
    repeat = 'no-repeat',
    size = '',
    overlayColor = ''
  } = bg || {};
  const styles = 'gradient' === type && isGradient ? isValidCSS('background', gradient) : 'image' === type && isImage ? `background: url(${image?.url});
				${isValidCSS('background-color', overlayColor)}
				${isValidCSS('background-position', position)}
				${isValidCSS('background-size', size)}
				${isValidCSS('background-repeat', repeat)}
				${isValidCSS('background-attachment', attachment)}
				${isValidCSS('background-repeat', repeat)}
				background-blend-mode: overlay;` : isSolid && isValidCSS('background', color);
  return styles;
};
const getBorderCSS = border => {
  const {
    width = '',
    style = 'solid',
    color = '',
    side = 'all',
    radius = ''
  } = border || {};
  const borderSideCheck = s => {
    const bSide = side?.toLowerCase();
    return bSide?.includes('all') || bSide?.includes(s);
  };
  const borderCSS = `${width} ${style} ${color}`;
  const styles = `
		${!width || parseInt(width) === 0 ? '' : ['top', 'right', 'bottom', 'left'].map(side => borderSideCheck(side) ? `border-${side}: ${borderCSS};` : '').join('')}
		${isValidCSS('border-radius', radius)}
	`;
  return styles;
};
const getBorderBoxCSS = border => {
  if (!border) return '';
  const generateBorderCSS = borderObj => {
    const {
      color = '#000000',
      style = 'solid',
      width = '0px'
    } = borderObj;
    return `${width} ${style} ${color}`;
  };
  if ('object' === typeof border && !Array.isArray(border)) {
    if (border.hasOwnProperty('top') || border.hasOwnProperty('right') || border.hasOwnProperty('bottom') || border.hasOwnProperty('left')) {
      const sides = ['top', 'right', 'bottom', 'left'];
      return sides.map(side => border[side] ? `border-${side}: ${generateBorderCSS(border[side])};` : '').join(' ').trim();
    } else {
      return isValidCSS('border', generateBorderCSS(border));
    }
  }
  return '';
};
const getColorsCSS = colors => {
  const {
    color = '',
    bgType = 'solid',
    bg = '',
    gradient = _data__WEBPACK_IMPORTED_MODULE_7__.gradient
  } = colors || {};
  const styles = `
		${isValidCSS('color', color)}
		${gradient || bg ? isValidCSS('background', 'gradient' === bgType ? gradient : bg) : ''}
	`;
  return styles;
};
const getIconCSS = (icon, isSize = true, isColor = true) => {
  const {
    fontSize = 16,
    colorType = 'solid',
    color = 'inherit',
    gradient = _data__WEBPACK_IMPORTED_MODULE_7__.gradient
  } = icon || {};
  const colorCSS = 'gradient' === colorType ? `color: transparent; background-image: ${gradient}; -webkit-background-clip: text; background-clip: text;` : isValidCSS('color', color);
  const styles = `
		${!fontSize || !isSize ? '' : isValidCSS('font-size', fontSize ? `${fontSize}px` : '')}
		${isColor ? colorCSS : ''}
	`;
  return styles;
};
const getMultiShadowCSS = (value, type = 'box') => {
  let styles = '';
  value?.map((item, index) => {
    const {
      hOffset = '0px',
      vOffset = '0px',
      blur = '0px',
      spreed = '0px',
      color = '#e7f0fe',
      isInset = false
    } = item || {};
    const inset = isInset ? 'inset' : '';
    const offsetBlur = `${hOffset} ${vOffset} ${blur}`;
    const isComa = index + 1 >= value.length ? '' : ', ';
    styles += 'text' === type ? `${offsetBlur} ${color}${isComa}` : `${offsetBlur} ${spreed} ${color} ${inset}${isComa}`;
  });
  return styles || '';
};
const getSeparatorCSS = separator => {
  const {
    width = '50%',
    height = '2px',
    style = 'solid',
    color = _data__WEBPACK_IMPORTED_MODULE_7__.primaryColor300
  } = separator || {};
  const styles = `
		${isValidCSS('width', width)}
		${0 === parseInt(height) ? '' : `border-top: ${height} ${style} ${color};`}
	`;
  return styles;
};
const getShadowCSS = (shadow, type = 'box') => {
  const {
    hOffset = '0px',
    vOffset = '0px',
    blur = '0px',
    spreed = '0px',
    color = '#7090b0',
    isInset = false
  } = shadow || {};
  const inset = isInset ? 'inset' : '';
  const offsetBlur = `${hOffset} ${vOffset} ${blur}`;
  const styles = 'text' === type ? `${offsetBlur} ${color}` : `${offsetBlur} ${spreed} ${color} ${inset}`;
  return styles || 'none';
};
const getSpaceCSS = space => {
  const {
    side = 2,
    vertical = '0px',
    horizontal = '0px',
    top = '0px',
    right = '0px',
    bottom = '0px',
    left = '0px'
  } = space || {};
  const styles = 2 === side ? `${vertical} ${horizontal}` : `${top} ${right} ${bottom} ${left}`;
  return styles;
};
const getTypoCSS = (selector, typo, isFamily = true) => {
  const {
    fontFamily = 'Default',
    fontCategory = 'sans-serif',
    fontVariant = 400,
    fontWeight,
    isUploadFont = true,
    fontSize = {
      desktop: null,
      tablet: null,
      mobile: null
    },
    fontStyle,
    textTransform,
    textDecoration,
    lineHeight,
    letterSpace
  } = typo || {};
  const isEmptyFamily = !isFamily || !fontFamily || 'Default' === fontFamily;
  const desktopFontSize = fontSize?.desktop || fontSize;
  const tabletFontSize = fontSize?.tablet || desktopFontSize;
  const mobileFontSize = fontSize?.mobile || tabletFontSize;
  const checkUnit = size => {
    const value = String(size);
    const units = ['px', 'em', 'rem', '%', 'vh', 'vw'];
    if (units.some(unit => value.endsWith(unit))) {
      return value;
    } else if (typeof size === 'number') {
      return `${value}px`;
    }
    return '';
  };
  const styles = `
		${isEmptyFamily ? '' : `font-family: '${fontFamily}', ${fontCategory};`}
		${isValidCSS('font-weight', fontWeight)}
		${isValidCSS('font-size', checkUnit(desktopFontSize))}
		${isValidCSS('font-style', fontStyle)}
		${isValidCSS('text-transform', textTransform)}
		${isValidCSS('text-decoration', textDecoration)}
		${isValidCSS('line-height', lineHeight)}
		${isValidCSS('letter-spacing', letterSpace)}
	`;

  // Google font link
  const linkQuery = !fontVariant || 400 === fontVariant ? '' : '400i' === fontVariant ? ':ital@1' : fontVariant?.includes('00i') ? `: ital, wght@1, ${fontVariant?.replace('00i', '00')} ` : `: wght@${fontVariant} `;
  const link = isEmptyFamily ? '' : `https://fonts.googleapis.com/css2?family=${fontFamily?.split(' ').join('+')}${linkQuery.replace(/ /g, '')}&display=swap`;
  return {
    googleFontLink: !isUploadFont || isEmptyFamily ? '' : `@import url(${link});`,
    styles: `${selector}{
			${styles}
		}
		${_data__WEBPACK_IMPORTED_MODULE_7__.tabBreakpoint} {
			${selector}{
				${isValidCSS('font-size', checkUnit(tabletFontSize))}
			}
		}
		${_data__WEBPACK_IMPORTED_MODULE_7__.mobileBreakpoint} {
			${selector}{
				${isValidCSS('font-size', checkUnit(mobileFontSize))}
			}
		}`.replace(/\s+/g, ' ').trim()
  };
};
const getBoxCSS = val => {
  if (!val) return '';
  if (typeof val === 'string') return val;
  if (typeof val === 'object' && !Array.isArray(val)) {
    const order = ['top', 'right', 'bottom', 'left'];
    const values = order.map(side => val[side] || '');
    const allEmpty = values.every(value => !value);
    return allEmpty ? '' : order.map(side => val[side] || '0').join(' ');
  }
  return '';
};

// Murad Wahid
const getGradientCSS = gradient => {
  const {
    type = 'linear',
    radialType = 'ellipse',
    colors = [],
    centerPositions = {
      x: 0,
      y: 0
    },
    angel = 0
  } = gradient || {};
  if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(gradient)) {
    const gradientColors = colors?.map(({
      color,
      position
    }) => `${color} ${position}%`);
    const liner = `linear-gradient(${angel}deg, ${gradientColors})`;
    const radial = `radial-gradient(${radialType} at ${centerPositions?.x}% ${centerPositions?.y}%,${gradientColors})`;
    return isValidCSS('background', type === 'radial' ? radial : liner);
  }
  return '';
};
const getImagePosition = img => {
  const {
    position = 'center center',
    xPosition = 0,
    yPosition = 0,
    attachment = '',
    repeat = 'no-repeat',
    size = 'cover',
    customSize = '0px'
  } = img || {};
  const cd = v => 'initial' !== v || (0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(v);
  return `
		${cd(position) ? `background-position: ${'custom' === position ? `${xPosition} ${yPosition}` : position};` : ''}
		${attachment && cd(attachment) ? `background-attachment: ${attachment};` : ''}
		${cd(repeat) ? `background-repeat: ${repeat};` : ''}
		${cd(size) ? `background-size: ${'custom' === size ? `${customSize} auto` : size};` : ''}
	`;
};
const getImageCSS = (img = {}) => {
  if (img) {
    return {
      desktop: (0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(img.url) ? `background-image: url(${img.url}); ${getImagePosition(img?.desktop)}` : '',
      tablet: (0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(img.url) ? getImagePosition(img?.tablet) : '',
      mobile: (0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(img.url) ? getImagePosition(img?.mobile) : ''
    };
  }
  return '';
};
const getVideoCSS = (video, selector) => {
  const {
    url,
    loop
  } = video || {};
  const parentEl = document.querySelector(selector);
  const el = parentEl?.querySelector('.bPlVideo');
  const videoEl = document.createElement('video');
  videoEl.muted = true;
  videoEl.autoplay = true;
  videoEl.classList.add('bPlVideo');
  if (!el) {
    if (parentEl && url) {
      videoEl.innerHTML = `<source src=${url}></source>`;
      parentEl.appendChild(videoEl);
    }
  }
  videoEl.loop = loop;
  videoEl.play();
  return `${selector} .bPlVideo{
		left: 0;
		min-height: 100%;
		min-width: 100%;
		position: absolute;
		width: -webkit-fill-available;
		top: 0;
		z-index: 0;
	}`;
};
const getAdvBGCSS = (background, selector, isHover = false) => {
  const {
    type = 'color',
    color,
    gradient,
    img,
    video
  } = background || {};
  const bgCSS = type === 'color' ? isValidCSS('background', color) : type === 'gradient' ? getGradientCSS(gradient) : type === 'image' ? getImageCSS(img).desktop : '';
  const tablet = type === 'image' ? getImageCSS(img).tablet : '';
  const mobile = type === 'image' ? getImageCSS(img).mobile : '';
  const sl = isHover ? `${selector}:hover` : selector;
  return `
		${type === 'video' ? getVideoCSS(video, selector) : ''}

		${bgCSS ? `${sl}{
			${bgCSS}
		}` : ''}

		${tablet ? `${_data__WEBPACK_IMPORTED_MODULE_7__.tabBreakpoint} {
			${sl}{
				${tablet}
			}
		}` : ''}

		${mobile ? `${_data__WEBPACK_IMPORTED_MODULE_7__.mobileBreakpoint} {
			${sl}{
				${mobile}
			}
		}` : ''}
	`.replace(/\s+/g, ' ').trim();
};
const getOverlayCSS = (overlay, selector, isHover = false) => {
  const {
    isEnabled,
    colors,
    opacity = 1,
    blend,
    filter = '',
    blur = 0,
    brightness = 100,
    contrast = 100,
    saturation = 100,
    hue = 0,
    position = {
      top: 0,
      right: 0,
      bottom: 0,
      left: 0
    },
    zIndex = -1
  } = overlay || {};
  const filterCSSValue = `${100 !== brightness ? `brightness(${brightness}%)` : ''} ${100 !== contrast ? `contrast(${contrast}%)` : ''} ${100 !== saturation ? `saturate(${saturation}%)` : ''} ${0 !== blur ? `blur(${blur}px)` : ''} ${0 !== hue ? `hue-rotate(${hue}deg)` : ''}`;
  const filterCSS = `${filter}: ${filter ? filterCSSValue : ''}; -webkit-${filter}: ${filter ? filterCSSValue : ''};`;
  const sl = isHover ? `${selector}:hover::after` : `${selector}::after`;
  return isEnabled ? `
		${selector}{
			position:relative;
			z-index:1;
		}
		${selector}::after{
			content: '';
			position: absolute;
			top:${(0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(position.top) ? position.top : 0};
			right:${(0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(position.right) ? position.right : 0};
			bottom:${(0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(position.bottom) ? position.bottom : 0};
			left:${(0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(position.left) ? position.left : 0};
			z-index:${zIndex};
		}
		${getAdvBGCSS(colors, sl, false)}
		${sl}{
			${isValidCSS('opacity', opacity)}
			${isValidCSS('mix-blend-mode', blend)}
			${filterCSS}
		}
	`.replace(/\s+/g, ' ').trim() : '';
};
const getTransformCSS = (transform, selector, isHover = false) => {
  const generateTransformCSS = (value, device = 'desktop') => {
    if (!(0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(value)) return '';
    const {
      skew,
      scale,
      rotate,
      offset,
      flipX,
      flipY
    } = value;
    const {
      threeDRotate
    } = rotate || {};
    const {
      isProportion
    } = scale || {};
    const transforms = [];
    //skew
    if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(skew)) {
      if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(skew?.[device]?.x)) transforms.push(`skewX(${skew[device].x}deg)`);
      if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(skew?.[device]?.y)) transforms.push(`skewY(${skew[device].y}deg)`);
    }
    //scale
    if (isProportion) {
      if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(scale?.[device]?.scale)) transforms.push(`scale(${scale[device].scale})`);
    } else {
      if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(scale?.[device]?.x)) transforms.push(`scaleX(${scale[device].x})`);
      if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(scale?.[device]?.y)) transforms.push(`scaleY(${scale[device].y})`);
    }

    //rotate
    if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(rotate)) {
      if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(rotate?.[device]?.z)) transforms.push(`rotateZ(${rotate[device].z}deg)`);
      if (threeDRotate) {
        if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(rotate?.[device]?.x)) transforms.push(`rotateX(${rotate[device].x}deg)`);
        if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(rotate?.[device]?.y)) transforms.push(`rotateY(${rotate[device].y}deg)`);
      }
    }

    //offset
    if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(offset)) {
      if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(offset?.[device]?.x)) transforms.push(`translateX(${offset[device].x})`);
      if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(offset?.[device]?.y)) transforms.push(`translateY(${offset[device].y})`);
    }

    //flip
    if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(flipX)) transforms.push(flipX ? 'scaleX(-1)' : '');
    if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(flipY)) transforms.push(flipY ? 'scaleY(-1)' : '');
    if (transforms.length === 0) return '';
    return isValidCSS('transform', transforms.join(' '));
  };
  const sl = isHover ? `${selector}:hover` : selector;
  // ${isExist(hover?.transition)?`transition:transform ${hover.transition}ms ease-in-out`:'' }

  return `
		${sl} {
			${generateTransformCSS(transform, 'desktop')}
		}
		${_data__WEBPACK_IMPORTED_MODULE_7__.tabBreakpoint}{
			${sl} {
				${generateTransformCSS(transform, 'tablet')}
			}
		}
		${_data__WEBPACK_IMPORTED_MODULE_7__.mobileBreakpoint}{
			${sl} {
				${generateTransformCSS(transform, 'mobile')}
			}
		}
	`;
};
const getMaskCSS = mask => {
  const {
    isMask = false,
    shape = {
      type: 'circle'
    },
    size = {
      type: 'contain',
      scale: '100%'
    },
    position = {
      type: 'center center',
      x: 50,
      y: 50
    },
    repeat = 'no-repeat'
  } = mask || {};
  const svgShape = [{
    svg: _Components_Mask_assets_shapes_circle_svg__WEBPACK_IMPORTED_MODULE_1__["default"],
    type: 'circle'
  }, {
    svg: _Components_Mask_assets_shapes_flower_svg__WEBPACK_IMPORTED_MODULE_2__["default"],
    type: 'flower'
  }, {
    svg: _Components_Mask_assets_shapes_sketch_svg__WEBPACK_IMPORTED_MODULE_4__["default"],
    type: 'sketch'
  }, {
    svg: _Components_Mask_assets_shapes_triangle_svg__WEBPACK_IMPORTED_MODULE_5__["default"],
    type: 'triangle'
  }, {
    svg: _Components_Mask_assets_shapes_blob_svg__WEBPACK_IMPORTED_MODULE_0__["default"],
    type: 'blob'
  }, {
    svg: _Components_Mask_assets_shapes_hexagon_svg__WEBPACK_IMPORTED_MODULE_3__["default"],
    type: 'hexagon'
  }];
  const getShape = type => svgShape.find(e => e.type === type);
  return isMask ? `-webkit-mask-image: url(${shape.type === 'custom' ? shape.url : getShape(shape.type).svg});
		-webkit-mask-size: ${size.type === 'custom' ? size.scale : size.type};
		${position.type === 'custom' ? `${isValidCSS('-webkit-mask-position-x', position.x)}
			${isValidCSS('-webkit-mask-position-y', position.y)}` : `${isValidCSS('-webkit-mask-position', position.type)}`}
		${size.type !== 'cover' ? `-webkit-mask-repeat: ${repeat};` : ''}` : '';
};

/***/ }),

/***/ "./src/Components/Common/Style.js":
/*!****************************************!*\
  !*** ./src/Components/Common/Style.js ***!
  \****************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../../bpl-tools/utils/common */ "../bpl-tools/utils/common.js");
/* harmony import */ var _bpl_tools_utils_data__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../../bpl-tools/utils/data */ "../bpl-tools/utils/data.js");
/* harmony import */ var _bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../../../bpl-tools/utils/getCSS */ "../bpl-tools/utils/getCSS.js");




const Style = ({
  attributes,
  id
}) => {
  const {
    members = [],
    columns,
    columnGap,
    rowGap,
    layout,
    textAlign,
    padding,
    photoWidth,
    photoMargin,
    nameTypo,
    nameMargin,
    titleTypo,
    titleMargin,
    sepMargin,
    bioTypo,
    bioMargin,
    socialSize,
    socialIconMargin,
    styles,
    theme = "default",
    options = {}
  } = attributes;
  const {
    hoverOnScale = true,
    isShowWaterMark = true
  } = options || {};
  const {
    bg = {},
    width = "100%",
    alignment = "center",
    padding: sectionPadding = {
      top: "0px",
      left: "0px",
      bottom: "0px",
      right: "0px"
    },
    margin = {
      top: "0px",
      left: "0px",
      bottom: "0px",
      right: "0px"
    },
    teamMember = {}
  } = styles || {};
  const mainSl = `#${id}`;
  const membersSl = `${mainSl} .tsbTeamMembers`;
  const gMemberSl = `${membersSl} .tsbMember`;
  const gMemberDetailsSl = `${gMemberSl} .memberDetails`;
  const gMemberSocialSl = `${gMemberDetailsSl} .memberSocial`;
  const tsbTeamMembersWrapperlSl = `${mainSl} .tsbTeamMembersWrapper`;
  const tsbTeamMembersTeamContainerSl = `${tsbTeamMembersWrapperlSl} .tsbTeamMembersTeamContainer`;
  const tsbTeamMembersTeamMemberSl = `${tsbTeamMembersTeamContainerSl} .tsbTeamMembersTeamMember`;
  const tsbTeamMembersTeamMemberThumbSl = `${tsbTeamMembersTeamMemberSl} .tsbTeamMembersTeamMemberThumb`;
  const tsbTeamMemberNameSl = `${mainSl} .tsbTeamMemberName`;
  const coFounderSl = `${tsbTeamMembersTeamContainerSl} .co-funder`;
  const tsbTeamMemberBioSl = `${tsbTeamMembersTeamMemberSl} .tsbTeamMemberBio`;
  const tsbTeamMemberUserNameSl = `${tsbTeamMembersTeamMemberSl} .tsbTeamMemberUserName`;
  const tsbTeamMembersWrapperTheme6Sl = `${mainSl} .tsbTeamMembersWrapperTheme-6`;
  const tsbTeamMembersWrapperTheme7Sl = `${mainSl} .tsbTeamMembersWrapperTheme-7`;
  const tsbTeamMembersWrapperTheme8Sl = `${mainSl} .tsbTeamMembersWrapperTheme-8`;
  const tsbTeamMembersWrapperTheme9Sl = `${mainSl} .tsbTeamMembersWrapperTheme-9`;
  const tsbTeamMembersWrapperTheme10Sl = `${mainSl} .tsbTeamMembersWrapperTheme-10`;
  const tsbTeamMembersWrapperTheme11Sl = `${mainSl} .tsbTeamMemberWrapperTheme-11`;
  // .threeDinfoCard${index}{
  // 	${getBackgroundCSS(card.backgroundImage)}
  // }
  // .threeDinfoCardContentBox${index}{
  // background:${card?.backgroundColor};
  // box-shadow:${getMultiShadowCSS(card?.shadow)};
  // padding:${getBoxCSS(styles?.card?.contentPadding)}
  // }
  // ${dateBoxSl}${index}{
  // box-shadow:${getMultiShadowCSS(card?.tagShadow)};

  // }
  const dynamicAlignment = members.map((card, index) => {
    return `${tsbTeamMembersWrapperTheme6Sl}  .tsbTeamMembersTeamMemberContentContainer-${index}{
			padding-top:${card?.paddingTop};
			}
					`;
  }).join("\n");
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("style", {
    dangerouslySetInnerHTML: {
      __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__.escapeHTML)(`
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)("", nameTypo)?.googleFontLink}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)("", titleTypo)?.googleFontLink}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)("", bioTypo)?.googleFontLink}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)("", teamMember?.name?.typo)?.googleFontLink}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)("", teamMember?.title?.typo)?.googleFontLink}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)("", teamMember?.bio?.typo)?.googleFontLink}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)("", teamMember?.userName?.typo)?.googleFontLink}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)("", styles?.waterMark?.typo)?.googleFontLink}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)("", teamMember?.teamRing?.typo)?.googleFontLink}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)("", teamMember?.serial?.typo)?.googleFontLink}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)("", teamMember?.states?.label?.typo)?.googleFontLink}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)("", teamMember?.states?.value?.typo)?.googleFontLink}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)("", teamMember?.btn?.typo)?.googleFontLink}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(`${gMemberDetailsSl} .memberName`, nameTypo)?.styles}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(`${gMemberDetailsSl} .memberTitle`, titleTypo)?.styles}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(`${gMemberDetailsSl} .memberBio`, bioTypo)?.styles}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(`${tsbTeamMemberNameSl}`, teamMember?.name?.typo)?.styles}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(`${tsbTeamMembersWrapperTheme7Sl} .team-ring label`, teamMember?.name?.typo)?.styles}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(`${tsbTeamMembersWrapperTheme11Sl} .tsbTeamMemberBio`, teamMember?.bio?.typo)?.styles}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(`${coFounderSl}:after`, teamMember?.title?.typo)?.styles} 
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(` ${tsbTeamMembersWrapperTheme7Sl} #center`, teamMember?.teamRing?.typo)?.styles} 
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(` ${tsbTeamMembersWrapperTheme10Sl} .tsbTeamMember_content_card_number_box`, teamMember?.serial?.typo)?.styles} 
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(` ${tsbTeamMembersWrapperTheme10Sl} .tsbTeamMember_content_card_stat_label`, teamMember?.states?.label?.typo)?.styles} 
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(` ${tsbTeamMembersWrapperTheme10Sl} .tsbTeamMember_content_card_stat_value`, teamMember?.states?.value?.typo)?.styles} 
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(` ${tsbTeamMembersWrapperTheme10Sl} .tsbTeamMember_btn`, teamMember?.btn?.typo)?.styles} 
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(` ${tsbTeamMembersWrapperTheme11Sl} .tsbTeamMember_view_btn`, teamMember?.btn?.typo)?.styles} 
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(`${tsbTeamMembersWrapperTheme6Sl} .tsbTeamMembersTeamContainer .tsbTeamMemberTitle`, teamMember?.title?.typo)?.styles} 
	${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(`${tsbTeamMembersWrapperTheme8Sl} .tsbTeamMembersTeamContainer .tsbTeamMemberTitle`, teamMember?.title?.typo)?.styles} 
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(`${tsbTeamMemberBioSl}`, teamMember?.bio?.typo)?.styles} 
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(`${tsbTeamMemberUserNameSl}`, teamMember?.userName?.typo)?.styles} 
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(`${tsbTeamMembersWrapperTheme6Sl} .tsbTeamMembersBgWatermark`, styles?.waterMark?.typo)?.styles} 
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getTypoCSS)(`.tsbTeamMemberTitle`, teamMember?.title?.typo)?.styles} 

		${membersSl}{
			grid-gap: ${rowGap} ${columnGap};
		}
		${gMemberSl}{
			text-align: ${textAlign};
			padding: ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getSpaceCSS)(padding)};
		}
		${gMemberSl} .memberPhoto{
			width: ${photoWidth};
			margin: ${"horizontal" === layout ? "0 20px 0 0" : `${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getSpaceCSS)(photoMargin)}`};
		}
		${gMemberDetailsSl} .memberName{
			margin: ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getSpaceCSS)(nameMargin)};
		}
		${gMemberDetailsSl} .memberTitle{
			margin: ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getSpaceCSS)(titleMargin)};
		}
		${gMemberDetailsSl} .memberSeparator{
			margin: ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getSpaceCSS)(sepMargin)};
		}
		${gMemberDetailsSl} .memberBio{
			margin: ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getSpaceCSS)(bioMargin)};
		}
		${gMemberSocialSl} .icon,
		${gMemberSocialSl} .memberSocialAdd{
			margin: ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getSpaceCSS)(socialIconMargin)};
		}
		${gMemberSocialSl} .icon i,
		${gMemberSocialSl} .memberSocialAdd i{
			font-size: ${socialSize};
			width: ${socialSize};
		}

		${members.map((member, index) => {
        const {
          background,
          border,
          shadow,
          photoBorder,
          nameColor,
          titleColor,
          separator,
          bioColor,
          socialIconColors
        } = member;
        const memberSl = `${mainSl} #tsbMember-${index}`;
        const memberDetailsSl = `${memberSl} .memberDetails`;
        const memberSocialSl = `${memberDetailsSl} .memberSocial`;
        return `
				${memberSl}{
					${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(background)}
					${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBorderCSS)(border)}
					box-shadow: ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getMultiShadowCSS)(shadow?.shadow || shadow)};
				}
				${memberSl} .memberPhoto{
					${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBorderCSS)(photoBorder)}
				}
				${memberDetailsSl} .memberName{
					color: ${nameColor};
				}
				${memberDetailsSl} .memberTitle{
					color: ${titleColor};
				}
				${memberSl} .memberSeparator{
					${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getSeparatorCSS)(separator)}
				}
				${memberDetailsSl} .memberBio{
					color: ${bioColor};
				}
				${memberSocialSl}{
					justify-content: ${"center" === textAlign ? "center" : "right" === textAlign ? "flex-end" : "flex-start"}
				}
				${memberSocialSl} li a{
					${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getColorsCSS)(socialIconColors)}
				}
				${memberSocialSl} li.isSelected{
					border: 2px solid ${socialIconColors?.bg};
				}
				${memberSocialSl} li.memberSocialAdd button i{
					color: ${socialIconColors?.bg};
				}
			`;
      }).join(" ")}

	  

		${tsbTeamMembersWrapperlSl}{
		justify-content: ${alignment};
		margin:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(margin)};
		
		}
		${tsbTeamMembersTeamContainerSl}{
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(bg)}
		width:${width};
		padding:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(sectionPadding)};
        grid-template-columns: repeat(${columns?.desktop},1fr);
		column-gap: ${columnGap}; 
        row-gap: ${rowGap};     
 
		}
		
		${tsbTeamMembersTeamMemberSl}{
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(teamMember?.bg)}
		padding:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.padding)};
		}
		${tsbTeamMembersTeamMemberSl}:nth-of-type(even){
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(teamMember?.evenItemBg)}
		}
			${tsbTeamMembersTeamMemberSl}:nth-of-type(even) .tsbTeamMembersTeamMemberThumb {
			${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(teamMember?.photo?.evenItemPhotoBg)}
			}

		${tsbTeamMembersTeamMemberThumbSl}{
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(teamMember?.photo?.bg)}
		width:${teamMember?.photo?.size};
		height:${teamMember?.photo?.size};
		border-radius:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.photo?.radius)};
		padding:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.photo?.padding)};
		}
		${tsbTeamMembersTeamMemberThumbSl} img {
		filter: grayscale(${teamMember?.photo?.grayScale});
		}
		${tsbTeamMemberNameSl}{
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getColorsCSS)(teamMember?.name?.colors)}
		padding:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.name?.padding)};
		border-radius:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.name?.radius)};
		}
		
		${coFounderSl}:after{
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getColorsCSS)(teamMember?.title?.colors)}
		padding:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.title?.padding)};
		border-radius:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.title?.radius)};
		}

		${tsbTeamMemberBioSl}{
		color:${teamMember?.bio?.color};
		}

		${tsbTeamMemberUserNameSl}{
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getColorsCSS)(teamMember?.userName?.colors)}
		padding:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.userName?.padding)};
		border-radius:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.userName?.radius)};
		}
		${mainSl}{
		
		${["theme6", "theme8", "theme9", "theme10", "theme11", "theme12"].includes(theme) && `
			display:flex;
		justify-content:${alignment};

			`}
		}
			


		${tsbTeamMembersWrapperTheme6Sl}{
		${isShowWaterMark && "padding-top:100px;"}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(bg)}
		width:${width};
	    margin:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(margin)};

		}

		${tsbTeamMembersWrapperTheme6Sl} .tsbTeamMembersTeamContainer{
		
		grid-template-columns: repeat(${columns?.desktop}, 1fr);
		column-gap: ${columnGap}; 
        row-gap: ${rowGap};
		padding:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(sectionPadding)};
		}

		${tsbTeamMembersWrapperTheme6Sl} .tsbTeamMembersTeamContainer .tsbTeamMembersTeamMember{
		 min-height:${teamMember?.height?.desktop};
		 img{
		 min-height: 100%;
         height: ${teamMember?.height?.desktop};
		 filter: grayscale(${teamMember?.grayScale});
		 }
		}


		${tsbTeamMembersWrapperTheme6Sl} .tsbTeamMembersTeamContainer .tsbTeamMembersTeamMember::after{
		 ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(teamMember?.bg)}
		}
		 ${tsbTeamMembersWrapperTheme6Sl} .tsbTeamMembersTeamContainer .tsbTeamMembersTeamMemberShape::before{
		  ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(teamMember?.shape?.bg)}
		   width: ${teamMember?.shape?.width};
          height: ${teamMember?.shape?.height};
		 }
		  ${tsbTeamMemberNameSl}{
			color:${teamMember?.name?.color};
			margin:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.name?.margin)};
			}
			${tsbTeamMembersWrapperTheme6Sl} .tsbTeamMembersTeamContainer .tsbTeamMemberTitle{
			color:${teamMember?.title?.color};
			margin:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.title?.margin)};
			}

			${tsbTeamMembersWrapperTheme6Sl} .tsbTeamMembersTeamContainer .tsbTeamMembersTeamMember .tsbTeamMemberSocial{
			                        margin:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.icon?.margin)};
									gap:${teamMember?.icon?.gap}px;
									i {
							color: ${teamMember?.icon?.color};
							font-size: ${teamMember?.icon?.size}px;

							&:hover {
							color: ${teamMember?.icon?.hoverColor};
							}
						}
			}

       ${tsbTeamMembersWrapperTheme6Sl}  .tsbTeamMembersBgWatermark{
	   color:${styles?.waterMark?.color || "#1f1f1f"};
	   transform: translateX(-50%) translateY(${styles?.waterMark?.translateY}) ;
	   

	   }

	   ${tsbTeamMembersWrapperTheme7Sl}{
	  ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(bg)}
	  padding:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(sectionPadding)};
	  margin:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(margin)};
	   }
	  ${tsbTeamMembersWrapperTheme7Sl} .team-ring label{
	   width: ${teamMember?.photo?.width || "88px"};
       height:${teamMember?.photo?.height || "88px"};
	   border-radius: ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.photo?.radius || {
        top: "50%",
        left: "50%",
        bottom: "50%",
        right: "50%"
      })};
	    color: ${teamMember?.name?.color};	
	   
	  }
		${tsbTeamMembersWrapperTheme7Sl} .team-ring img {
				width: ${teamMember?.photo?.width || "88px"};
				height: ${teamMember?.photo?.height || "88px"};
				border-radius: ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.photo?.radius || {
        top: "50%",
        left: "50%",
        bottom: "50%",
        right: "50%"
      })};
				object-fit: ${teamMember?.photo?.object};
				
				}
		${tsbTeamMembersWrapperTheme7Sl} .team-ring input:checked + label img {
					transform: scale(${teamMember?.photo?.checkedScale || 2});
					}
					${tsbTeamMembersWrapperTheme7Sl} .team-ring input:checked + label svg {
						
						transform: translate(-50%, -50%) scale(${teamMember?.name?.textScale || 2.30}) rotate(${teamMember?.name?.rotate || 90}deg);
						
						}
		 ${tsbTeamMembersWrapperTheme7Sl} #center{
					width: ${teamMember?.teamRing?.width || '150px'};
					height: ${teamMember?.teamRing?.height || '150px'};
					${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getColorsCSS)(teamMember?.teamRing?.bg)}

					border-radius: ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.teamRing?.radius)};
							}
		${tsbTeamMembersWrapperTheme8Sl}{
		 ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(bg)}
		width:${width};
	    margin:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(margin)};
		}
			
	  ${tsbTeamMembersWrapperTheme8Sl} .tsbTeamMembersTeamContainer{
	  grid-template-columns: repeat(${columns?.desktop},1fr);
	  column-gap: ${columnGap}; 
      row-gap: ${rowGap};
	  padding:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(sectionPadding)};
	 
	  }

   ${tsbTeamMembersWrapperTheme8Sl} .tsbTeamMembersTeamContainer .tsbTeamMember .tsbTeamMember_img_wrapper img{
     width:${teamMember.memberPhoto?.width};
     height:${teamMember.memberPhoto?.height};
     object-fit: ${teamMember.memberPhoto?.objectFit}; 
	 filter: grayscale(${teamMember.memberPhoto?.grayScale});
	 

   }
		 ${tsbTeamMembersWrapperTheme8Sl} .tsbTeamMembersTeamContainer .tsbTeamMember .tsbTeamMember_img_wrapper:hover{
		 img {
          filter: grayscale(${teamMember.memberPhoto?.hoverGrayScale});
        }
		 }

		 ${tsbTeamMembersWrapperTheme8Sl} .tsbTeamMembersTeamContainer .tsbTeamMember .social-icon{
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(teamMember?.social?.bg)}
		padding:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.social?.padding)};
		 i{
		 color:${teamMember?.social?.color};
		 font-size:${teamMember?.social?.size}px;
		 }
		   &:hover i {
          color: ${teamMember?.social?.hoverColor} !important;
        }
		 }
		${tsbTeamMembersWrapperTheme8Sl} .tsbTeamMembersTeamContainer .tsbTeamMember .tsbTeamMember_content_wrapper .tsbTeamMember_content{
	    ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(teamMember?.content?.bg)}
		padding:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.content?.padding)};
		 svg {
              width: ${teamMember?.icon?.size}px;
              height: ${teamMember?.icon?.size}px;
              fill: ${teamMember?.icon?.color};
              color: ${teamMember?.icon?.color};
    
            }
		}
		${tsbTeamMembersWrapperTheme8Sl} .tsbTeamMembersTeamContainer .tsbTeamMember:hover .tsbTeamMember_content_wrapper .tsbTeamMember_content{
	    ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(teamMember?.content?.hoverBg)}
		  h3,
        p,
        svg {
          color:${teamMember?.content?.hoverColor};
          fill:${teamMember?.content?.hoverColor};

        }
		}
		${tsbTeamMembersWrapperTheme8Sl} .tsbTeamMembersTeamContainer .tsbTeamMember .tsbTeamMember_content_wrapper .tsbTeamMember_content .tsbTeamMemberName{
		 color:${teamMember?.name?.color};
		 
		}
		 ${tsbTeamMembersWrapperTheme8Sl} .tsbTeamMembersTeamContainer .tsbTeamMember .tsbTeamMember_content_wrapper .tsbTeamMember_content .tsbTeamMemberTitle{
		 color:${teamMember?.title?.color};
		 margin:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.title?.margin)};
		}

        ${tsbTeamMembersWrapperTheme9Sl}{
		 ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(styles?.bg)}
		 width:${styles?.width};
		 margin:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(styles?.margin)};

		
		}
		${tsbTeamMembersWrapperTheme9Sl} .tsbTeamMembersTeamContainer{
		 padding:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(sectionPadding)};
		grid-template-columns: repeat(${columns.desktop}, 1fr);
		column-gap:${columnGap};
		row-gap:${rowGap};
		} 
		${tsbTeamMembersWrapperTheme9Sl} .tsbTeamMembersTeamContainer .tsbTeamMember .tsbTeamMember_img_wrapper {
		height:${teamMember?.memberPhoto?.height};
		img{
		object-fit: ${teamMember?.memberPhoto?.objectFit};
		 filter: grayscale(${teamMember?.memberPhoto?.grayScale});
		}
		}
		${tsbTeamMembersWrapperTheme9Sl} .tsbTeamMembersTeamContainer .tsbTeamMember .tsbTeamMember_img_wrapper:hover{
		img{
		 filter: grayscale(${teamMember?.memberPhoto?.hoverGrayScale});
		  transform: scale(${teamMember?.memberPhoto?.hoverScale});
		}

		}
		 ${tsbTeamMembersWrapperTheme9Sl} .tsbTeamMembersTeamContainer .tsbTeamMember .tsbTeamMember_img_wrapper .tsbTeamMember_social_wrapper{
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(teamMember?.memberPhoto?.bg)}

		 }
        ${tsbTeamMembersWrapperTheme9Sl}  .tsbTeamMembersTeamContainer .tsbTeamMember .tsbTeamMember_social_wrapper .tsbTeamMember_social .tsbMember_social_link{
	    width: ${teamMember?.social?.width};
        height: ${teamMember?.social?.height};
        border-radius: ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.social?.radius)};
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getColorsCSS)(teamMember?.social?.colors)}
		i{font-size:${teamMember?.social?.size}px;}
        
		}
		${tsbTeamMembersWrapperTheme9Sl}  .tsbTeamMembersTeamContainer .tsbTeamMember .tsbTeamMember_social_wrapper .tsbTeamMember_social .tsbMember_social_link:hover{
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getColorsCSS)(teamMember?.social?.hoverColors)}
		} 
		${tsbTeamMembersWrapperTheme9Sl} .tsbTeamMemberName{
		color:${teamMember?.name?.color};
		margin:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.name?.margin)};
		} 
		${tsbTeamMembersWrapperTheme9Sl} .tsbTeamMemberTitle{
		color:${teamMember?.title?.color};
		margin:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.title?.margin)};
		}

		${tsbTeamMembersWrapperTheme11Sl}{
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(styles?.bg)}
		width:${styles?.width};
		padding:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(styles?.padding)};
		margin:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(styles?.margin)};
		  .tsbTeamMember-grid {
		  grid-template-columns: repeat(${columns?.desktop},1fr);
		  column-gap:${columnGap};
		  row-gap:${rowGap};
		   
		   .tsbTeamMember-card{
            ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(teamMember?.content?.bg)}
			border-radius:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.content?.radius)};
			box-shadow:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getMultiShadowCSS)(teamMember?.content?.shadow)};
			.tsbTeamMemberTitle{
			color:${teamMember?.title?.color};
			margin:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.title?.margin)};
			}
			.tsbTeamMemberBio{
			color:${teamMember?.bio?.color};
			}
			.tsbTeamMember-gradient-overlay {
			${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(teamMember?.memberPhoto?.bg)}
			}
			.tsbTeamMember_view_btn{
			color:${teamMember?.btn?.color};
			gap:${teamMember?.btn?.gap}px;
			  svg {
         
          width: ${teamMember?.btn?.iconSize}px;
          height: ${teamMember?.btn?.iconSize}px;
        }
        span{
          width: ${teamMember?.btn?.iconSize}px;
          height: ${teamMember?.btn?.iconSize}px;
        }
			}
		.tsbTeamMember-img {
		height:${teamMember?.memberPhoto?.height};
		object-fit:${teamMember?.memberPhoto?.objectFit};
		
		}
			
		   }
		  }
		}


${tsbTeamMembersWrapperTheme11Sl} .tsbTeamMember-card:hover{
 .tsbTeamMember-img {
        transform: scale(${teamMember?.memberPhoto?.hoverScale});
       
      }
}







		${_bpl_tools_utils_data__WEBPACK_IMPORTED_MODULE_2__.deskBreakpoint}{
		${theme === "theme6" && dynamicAlignment}
		${tsbTeamMembersTeamMemberSl}:hover{
		transform: scale(${hoverOnScale ? "1" : "0.85"});
		}
		}

			${_bpl_tools_utils_data__WEBPACK_IMPORTED_MODULE_2__.tabBreakpoint} {
				${tsbTeamMembersWrapperTheme11Sl}{
		  .tsbTeamMember-grid {
		  grid-template-columns: repeat(${columns?.tablet},1fr);
		  }
		}

		${theme === "theme6" && dynamicAlignment}

			${tsbTeamMembersTeamContainerSl} {
				grid-template-columns: repeat(${columns?.tablet}, 1fr);


			}

			${tsbTeamMembersWrapperTheme6Sl} .tsbTeamMembersTeamContainer {
				grid-template-columns: repeat(${columns?.tablet}, 1fr);
			}
					${tsbTeamMembersWrapperTheme6Sl} .tsbTeamMembersTeamContainer .tsbTeamMembersTeamMember{
		 min-height:${teamMember?.height?.tablet};
		 img{
		 min-height: 100%;
         height: ${teamMember?.height?.tablet};
		 filter: grayscale(${teamMember?.grayScale});
		 }
		}
		 ${tsbTeamMembersWrapperTheme8Sl} .tsbTeamMembersTeamContainer{
	  grid-template-columns: repeat(${columns?.tablet},1fr);
	  }
	  ${tsbTeamMembersWrapperTheme9Sl} .tsbTeamMembersTeamContainer{
		grid-template-columns: repeat(${columns.tablet}, 1fr);
		}

			}
		${tsbTeamMembersWrapperTheme10Sl}{
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(styles?.bg)}
		margin:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(styles?.margin)};
		padding:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(styles?.padding)};
		width:${styles?.width};
		}
		${tsbTeamMembersWrapperTheme10Sl}  {
		.tsbTeamMember_img_wrap{
		 max-width:${teamMember?.memberPhoto?.width};
            
		   .tsbTeamMember_img_shadow{
		   ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(teamMember?.memberPhoto?.bg)}

		   }

		  .tsbTeamMember_img_profile {
		  height:${teamMember?.memberPhoto?.height};
		  object-fit: ${teamMember?.memberPhoto?.objectFit};
		  }
		}
		
		  .tsbTeamMember_content_card{
		   ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBackgroundCSS)(teamMember?.content?.bg)}
		  padding:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.content?.padding)};
		  border-radius:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.content?.radius)};
		  box-shadow:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getMultiShadowCSS)(teamMember?.content?.shadow)};
		  
		  }

		  .team-row:hover .tsbTeamMember_img_shadow {
          transform: rotate(${teamMember?.content?.hoverRotate}deg);
        }
		  .tsbTeamMember_content_card_number_box{
		  ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getColorsCSS)(teamMember?.serial?.colors)}
          width: ${teamMember?.serial?.width};
          height: ${teamMember?.serial?.height};
          border-radius: ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.serial?.radius)};
		  }
		  .tsbTeamMemberTitle{
		  color:${teamMember?.title?.color};
		  margin:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.title?.margin)};
		  }
		  .tsbTeamMember_content_card_stats_grid{
		   border-top:${teamMember?.states?.border?.top?.width} ${teamMember?.states?.border?.top?.style} ${teamMember?.states?.border?.top?.color};
		   border-right:${teamMember?.states?.border?.right?.width} ${teamMember?.states?.border?.right?.style} ${teamMember?.states?.border?.right?.color};
		   border-bottom:${teamMember?.states?.border?.bottom?.width} ${teamMember?.states?.border?.bottom?.style} ${teamMember?.states?.border?.bottom?.color};
		   border-left:${teamMember?.states?.border?.left?.width} ${teamMember?.states?.border?.bottom?.style} ${teamMember?.states?.border?.bottom?.color};
		   padding:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.states?.padding)};
		   margin:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.states?.margin)};
		   
		  }
		   .tsbTeamMember_content_card_stat{
		   width:${teamMember?.states?.width};
		   height:${teamMember?.states?.height};
		   border-radius:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.states?.radius)};
		   svg{
           width:${teamMember?.states?.size}px;
		   height:${teamMember?.states?.size}px;
		   }

		   }
		   .tsbTeamMember_content_card_stat_value{
		   color:${teamMember?.states?.value?.color};
		   }
		   .tsbTeamMember_content_card_stat_label{
		   color:${teamMember?.states?.label?.color};

		   }
		   .tsbTeamMember_content_card_stat_one{
		   ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getColorsCSS)(teamMember?.states?.iconOne?.colors)}
		    svg{
			fill:${teamMember?.states?.iconOne?.colors?.color};
			
			}
		   }
			   .tsbTeamMember_content_card_stat_two{
		   ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getColorsCSS)(teamMember?.states?.iconTwo?.colors)}
		    svg{
			fill:${teamMember?.states?.iconTwo?.colors?.color};
			
			}
		   }
			.tsbTeamMember_btn{
			 ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getColorsCSS)(teamMember?.btn?.colors)}
			 padding:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.btn?.padding)};
			 border-radius:${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_3__.getBoxCSS)(teamMember?.btn?.radius)};
			}
		}



		${_bpl_tools_utils_data__WEBPACK_IMPORTED_MODULE_2__.mobileBreakpoint}{
			${tsbTeamMembersWrapperTheme11Sl}{
		  .tsbTeamMember-grid {
		  grid-template-columns: repeat(${columns?.mobile},1fr);
		  }
		}

		${tsbTeamMembersTeamContainerSl}{
		 grid-template-columns: repeat(${columns?.mobile},1fr);

		}

		${tsbTeamMembersWrapperTheme6Sl} .tsbTeamMembersTeamContainer{
		grid-template-columns: repeat(${columns?.mobile}, 1fr);
		}

		 ${tsbTeamMembersWrapperTheme6Sl} .tsbTeamMembersTeamContainer .tsbTeamMembersTeamMember{
		 min-height:${teamMember?.height?.mobile};
		 img{
		 min-height: 100%;
         height: ${teamMember?.height?.mobile};
		 }
		}
		   ${members.map((card, index) => {
        return `
        ${tsbTeamMembersWrapperTheme6Sl} .tsbTeamMembersTeamMemberContentContainer-${index}{
          padding-top: 0 !important;
        }
      `;
      }).join("\n")}
		 ${tsbTeamMembersWrapperTheme8Sl} .tsbTeamMembersTeamContainer{
	  grid-template-columns: repeat(${columns?.mobile},1fr);
	  }
	  ${tsbTeamMembersWrapperTheme9Sl} .tsbTeamMembersTeamContainer{
		grid-template-columns: repeat(${columns.mobile}, 1fr);
		}

		}
		`).replace(/\s+/g, " ")
    }
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Style);

/***/ }),

/***/ "./src/Components/Common/themes/Theme10.js":
/*!*************************************************!*\
  !*** ./src/Components/Common/themes/Theme10.js ***!
  \*************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../../../bpl-tools/utils/common */ "../bpl-tools/utils/common.js");
/* harmony import */ var _bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../../../bpl-tools/utils/functions */ "../bpl-tools/utils/functions.js");



const Theme10 = ({
  attributes,
  ReusableRichText,
  setAttributes
}) => {
  const {
    members = [],
    options = {},
    isLinkNewTab = "false",
    isSocial = true,
    isTitle = true
  } = attributes || {};
  const {
    isShowSereal = true,
    isShowBgShape = true
  } = options;
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("section", {
    className: "tsbTeamMembersWrapperTheme-10"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", null, members.map((member, index) => {
    const {
      label = "Connect on LinkedIn",
      link = "#"
    } = member?.btn || {};
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      key: index,
      className: `team-row ${index % 2 === 1 ? "reverse" : ""}`
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "tsbTeamMember_img_wrap"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "tsbTeamMember_img_box"
    }, isShowBgShape && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "tsbTeamMember_img_shadow"
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
      className: "tsbTeamMember_img_profile",
      src: member?.photo?.url || "https://images.pexels.com/photos/1516680/pexels-photo-1516680.jpeg?auto=compress&cs=tinysrgb&w=400",
      alt: ""
    }))), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "tsbTeamMember_content_card"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "profile-header"
    }, isShowSereal && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "tsbTeamMember_content_card_number_box"
    }, index + 1), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", null, ReusableRichText && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
      className: "tsbTeamMemberName",
      tagName: "p",
      value: member?.name,
      placeholder: "name...",
      onChange: v => setAttributes({
        members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__.updateData)(members, v, index, "name")
      })
    }), !ReusableRichText && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
      className: "tsbTeamMemberName",
      dangerouslySetInnerHTML: {
        __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__.sanitizeHTML)(member?.name || "")
      }
    }), ReusableRichText && isTitle && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
      className: "tsbTeamMemberTitle",
      tagName: "p",
      value: member?.title,
      placeholder: "title...",
      onChange: v => setAttributes({
        members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__.updateData)(members, v, index, "title")
      })
    }), !ReusableRichText && isTitle && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
      className: "tsbTeamMemberTitle",
      dangerouslySetInnerHTML: {
        __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__.sanitizeHTML)(member?.title || "")
      }
    }))), isSocial && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "tsbTeamMember_content_card_stats_grid"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "stat-box"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "tsbTeamMember_content_card_stat tsbTeamMember_content_card_stat_one"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      dangerouslySetInnerHTML: {
        __html: member?.state?.stateOne?.icon || "<svg width=\"32\" height=\"32\" stroke=\"currentColor\" fill=\"none\" stroke-width=\"2\" viewBox=\"0 0 24 24\"><path d=\"M6 3v5a6 6 0 0 0 12 0V3\" /><path d=\"M6 3h12\" /><path d=\"M6 8h12\" /><path d=\"M9 21l3-3 3 3\" /></svg>"
      }
    })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", null, ReusableRichText && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
      className: "tsbTeamMember_content_card_stat_value",
      tagName: "p",
      value: member?.state?.stateOne?.value,
      placeholder: "value...",
      onChange: v => setAttributes({
        members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__.updateData)(members, v, index, "state", "stateOne", "value")
      })
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
      className: "tsbTeamMember_content_card_stat_label",
      tagName: "p",
      value: member?.state?.stateOne?.label,
      placeholder: "label...",
      onChange: v => setAttributes({
        members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__.updateData)(members, v, index, "state", "stateOne", "label")
      })
    })), !ReusableRichText && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
      className: "tsbTeamMember_content_card_stat_value",
      dangerouslySetInnerHTML: {
        __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__.sanitizeHTML)(member?.state?.stateOne?.value || "")
      }
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
      className: "tsbTeamMember_content_card_stat_label",
      dangerouslySetInnerHTML: {
        __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__.sanitizeHTML)(member?.state?.stateOne?.label || "")
      }
    })))), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "stat-box"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "tsbTeamMember_content_card_stat tsbTeamMember_content_card_stat_two"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      dangerouslySetInnerHTML: {
        __html: member?.state?.stateTwo?.icon || "<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"32\" height=\"32\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><polygon points=\"13 2 3 14 12 14 11 22 21 10 12 10 13 2\"></polygon></svg>"
      }
    })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", null, ReusableRichText && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
      className: "tsbTeamMember_content_card_stat_value",
      tagName: "p",
      value: member?.state?.stateTwo?.value,
      placeholder: "value...",
      onChange: v => setAttributes({
        members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__.updateData)(members, v, index, "state", "stateTwo", "value")
      })
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
      className: "tsbTeamMember_content_card_stat_label",
      tagName: "p",
      value: member?.state?.stateTwo?.label,
      placeholder: "label...",
      onChange: v => setAttributes({
        members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__.updateData)(members, v, index, "state", "stateTwo", "label")
      })
    })), !ReusableRichText && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
      className: "tsbTeamMember_content_card_stat_value",
      dangerouslySetInnerHTML: {
        __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__.sanitizeHTML)(member?.state?.stateTwo?.value || "")
      }
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
      className: "tsbTeamMember_content_card_stat_label",
      dangerouslySetInnerHTML: {
        __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__.sanitizeHTML)(member?.state?.stateTwo?.label || "")
      }
    }))))), ReusableRichText && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "tsbTeamMember_btn_wrap"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
      className: "tsbTeamMember_btn",
      tagName: "span",
      value: label,
      placeholder: "btn text",
      onChange: v => setAttributes({
        members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__.updateData)(members, v, index, "btn", "label")
      })
    })), !ReusableRichText && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("a", {
      href: link,
      target: isLinkNewTab ? "_blank" : "_self",
      rel: "noreferrer",
      style: {
        textDecoration: "none",
        color: "inherit"
      }
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("button", {
      className: "tsbTeamMember_btn",
      dangerouslySetInnerHTML: {
        __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__.sanitizeHTML)(label)
      }
    }))));
  })));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Theme10);

/***/ }),

/***/ "./src/Components/Common/themes/Theme11.js":
/*!*************************************************!*\
  !*** ./src/Components/Common/themes/Theme11.js ***!
  \*************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../../../bpl-tools/utils/common */ "../bpl-tools/utils/common.js");
/* harmony import */ var _bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../../../bpl-tools/utils/functions */ "../bpl-tools/utils/functions.js");



const Theme11 = ({
  attributes,
  ReusableRichText,
  setAttributes
}) => {
  const {
    members = [],
    isLinkNewTab = "false",
    isTitle = true
  } = attributes || {};
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("section", {
    className: "tsbTeamMemberWrapperTheme-11"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "tsbTeamMember-grid"
  }, members.map((member, index) => {
    const {
      name,
      title,
      photo,
      bio,
      btn
    } = member || {};
    const {
      label = "view profile",
      link = "#",
      icon = "<svg width=\"20\" height=\"20\" fill=\"none\" stroke=\"currentColor\" viewBox=\"0 0 24 24\"><path d=\"M5 12h14\" /><path d=\"M12 5l7 7-7 7\" /></svg>"
    } = btn || {};
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      key: index,
      className: "tsbTeamMember-card"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "tsbTeamMember-inner"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "tsbTeamMember-img-box"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
      className: "tsbTeamMember-img",
      src: photo?.url || "https://images.pexels.com/photos/1516680/pexels-photo-1516680.jpeg?auto=compress&cs=tinysrgb&w=400",
      alt: ""
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "tsbTeamMember-gradient-overlay"
    })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "tsbTeamMember-text-box"
    }, isTitle && !ReusableRichText && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
      className: "tsbTeamMemberTitle",
      dangerouslySetInnerHTML: {
        __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__.sanitizeHTML)(title || "")
      }
    }), isTitle && ReusableRichText && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
      className: "tsbTeamMemberTitle",
      value: title,
      tagName: "p",
      placeholder: "title...",
      onChange: v => setAttributes({
        members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__.updateData)(members, v, index, "title")
      })
    }), ReusableRichText ? (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
      className: "tsbTeamMemberName",
      value: name,
      placeholder: "name...",
      tagName: "h3",
      onChange: v => setAttributes({
        members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__.updateData)(members, v, index, "name")
      })
    }) : (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h3", {
      className: "tsbTeamMemberName",
      dangerouslySetInnerHTML: {
        __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__.sanitizeHTML)(name || "")
      }
    }), ReusableRichText ? (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
      className: "tsbTeamMemberBio",
      value: bio,
      placeholder: "bio...",
      tagName: "p",
      onChange: v => setAttributes({
        members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__.updateData)(members, v, index, "bio")
      })
    }) : (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
      className: "tsbTeamMemberBio",
      dangerouslySetInnerHTML: {
        __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__.sanitizeHTML)(bio || "")
      }
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("a", {
      style: {
        textDecoration: "none",
        border: "none",
        boxShadow: "none",
        outline: "none"
      },
      target: isLinkNewTab ? "_blank" : "_self",
      rel: "noreferrer",
      href: link,
      className: "tsbTeamMember_view_btn"
    }, label, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      dangerouslySetInnerHTML: {
        __html: icon
      }
    })))));
  })));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Theme11);

/***/ }),

/***/ "./src/Components/Common/themes/Theme5.js":
/*!************************************************!*\
  !*** ./src/Components/Common/themes/Theme5.js ***!
  \************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../../../bpl-tools/utils/functions */ "../bpl-tools/utils/functions.js");
/* harmony import */ var _bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../../../bpl-tools/utils/common */ "../bpl-tools/utils/common.js");



const Theme5 = ({
  attributes,
  ReusableRichText,
  setAttributes
}) => {
  const {
    members = [],
    isLinkNewTab = false,
    isTitle = true,
    isBio = true,
    options = {}
  } = attributes || {};
  const {
    showUserName = true
  } = options || {};
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "tsbTeamMembersWrapper "
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("ul", {
    className: "tsbTeamMembersTeamContainer"
  }, members?.map((member, index) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("li", {
    key: index,
    className: `tsbTeamMembersTeamMember ${isTitle && "co-funder"}`,
    "data-role": member?.title
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "tsbTeamMembersTeamMemberThumb"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
    src: member?.photo?.url || "https://templates.bplugins.com/wp-content/uploads/2025/11/team-section-member-1.png"
  })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "tsbTeamMembersTeamMemberThumbDescription"
  }, ReusableRichText ? (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
    placeholder: " name ...",
    onChange: value => setAttributes({
      members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_1__.updateData)(members, value, index, "name")
    }),
    tagName: "h3",
    className: "tsbTeamMemberName",
    value: member?.name
  }) : (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h3", {
    className: "tsbTeamMemberName",
    dangerouslySetInnerHTML: {
      __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_2__.sanitizeHTML)(member?.name)
    }
  }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", null, ReusableRichText && isBio && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
    tagName: "span",
    placeholder: " bio...",
    className: "tsbTeamMemberBio",
    value: member?.bio,
    onChange: value => setAttributes({
      members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_1__.updateData)(members, value, index, "bio")
    })
  }), !ReusableRichText && isBio && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
    className: "tsbTeamMemberBio",
    dangerouslySetInnerHTML: {
      __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_2__.sanitizeHTML)(member?.bio)
    }
  }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("br", null), ReusableRichText && showUserName && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
    placeholder: " username...",
    className: "tsbTeamMemberUserName",
    value: member?.userName,
    tagName: "a",
    onChange: value => setAttributes({
      members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_1__.updateData)(members, value, index, "userName")
    })
  }), !ReusableRichText && showUserName && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("a", {
    className: "tsbTeamMemberUserName",
    href: member?.userNameLink || "",
    target: isLinkNewTab ? "_blank" : "_self",
    rel: "noreferrer",
    dangerouslySetInnerHTML: {
      __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_2__.sanitizeHTML)(member?.userName)
    }
  })))))));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Theme5);

/***/ }),

/***/ "./src/Components/Common/themes/Theme6.js":
/*!************************************************!*\
  !*** ./src/Components/Common/themes/Theme6.js ***!
  \************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../../../bpl-tools/utils/common */ "../bpl-tools/utils/common.js");
/* harmony import */ var _bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../../../bpl-tools/utils/functions */ "../bpl-tools/utils/functions.js");



const Theme6 = ({
  attributes,
  ReusableRichText,
  setAttributes
}) => {
  const {
    members = [],
    isLinkNewTab = false,
    isTitle = true,
    options = {},
    isSocial = true
  } = attributes || {};
  const {
    isShowWaterMark = true,
    isShowShape = true,
    waterMark = "TEAM"
  } = options;
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("section", {
    className: "tsbTeamMembersWrapperTheme-6"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", null, isShowWaterMark && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
    className: "tsbTeamMembersBgWatermark"
  }, waterMark), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "tsbTeamMembersTeamContainer"
  }, members.map((member, index) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    key: index,
    className: `tsbTeamMembersTeamMemberContentContainer-${index}`
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    key: index,
    className: `tsbTeamMembersTeamMember  ${isShowShape && "tsbTeamMembersTeamMemberShape"}`
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
    src: member?.photo?.url || "https://templates.bplugins.com/wp-content/uploads/2025/11/team-section-member-1.png",
    alt: "Antonia Moore"
  }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "tsbTeamMembersTeamMember-content"
  }, ReusableRichText ? (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
    placeholder: " name ...",
    onChange: value => setAttributes({
      members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__.updateData)(members, value, index, "name")
    }),
    tagName: "h3",
    className: "tsbTeamMemberName",
    value: member?.name
  }) : (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h3", {
    className: "tsbTeamMemberName",
    dangerouslySetInnerHTML: {
      __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__.sanitizeHTML)(member?.name)
    }
  }), isTitle && ReusableRichText ? (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
    placeholder: " title ...",
    onChange: value => setAttributes({
      members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__.updateData)(members, value, index, "title")
    }),
    tagName: "p",
    className: "tsbTeamMemberTitle",
    value: member?.title
  }) : (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
    className: "tsbTeamMemberTitle",
    dangerouslySetInnerHTML: {
      __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__.sanitizeHTML)(member?.title)
    }
  }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("ul", {
    className: "tsbTeamMemberSocial"
  }, isSocial && member?.social?.map((social, i) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("li", {
    key: i
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("a", {
    href: social?.link,
    target: isLinkNewTab ? "_blank" : "_self",
    rel: "noreferrer"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("i", {
    className: social?.icon?.class
  }))))))))))));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Theme6);

/***/ }),

/***/ "./src/Components/Common/themes/Theme7.js":
/*!************************************************!*\
  !*** ./src/Components/Common/themes/Theme7.js ***!
  \************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../../../bpl-tools/utils/functions */ "../bpl-tools/utils/functions.js");



const Theme7 = ({
  attributes,
  ReusableRichText,
  setAttributes
}) => {
  const {
    members = [],
    options = {}
  } = attributes || {};
  const {
    isShowWaterMark = true,
    waterMark = "THE TEAM"
  } = options;
  const radiosRef = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)([]);
  const handleRadioClick = index => e => {
    e.preventDefault();
    const radio = radiosRef.current[index];
    setTimeout(() => {
      radio.checked = !radio.checked;
    }, 0);
  };
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "tsbTeamMembersWrapperTheme-7"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "team-ring",
    style: {
      "--total": members.length,
      "--radius": `${Math.min(20, members.length) * 0.7}rem`,
      "--avatar-size": `${Math.max(40, 100 - members.length)}px`
    }
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    id: "center"
  }, isShowWaterMark && ReusableRichText && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
    value: waterMark,
    tagName: "span",
    onChange: val => setAttributes({
      options: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_1__.updateData)(options, val, "waterMark")
    })
  }), isShowWaterMark && !ReusableRichText && waterMark), members.map((member, index) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("input", {
    className: "tsbInputTpye",
    type: "radio",
    name: "avatar",
    id: `r${index}`,
    hidden: true,
    ref: el => radiosRef.current[index] = el,
    onClick: handleRadioClick(index),
    key: index
  }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("label", {
    className: "avatar",
    htmlFor: `r${index}`,
    style: {
      "--i": index
    }
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
    src: member?.photo?.url || 'https://templates.bplugins.com/wp-content/uploads/2025/11/team-section-member-2.png',
    alt: member.name
  }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
    viewBox: "0 0 300 300"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("text", {
    fill: "currentColor"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("textPath", {
    xlinkHref: "#circlePath",
    style: {
      zIndex: 999999999999999
    }
  }, member.name, " - ", member.title))))))), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
    width: "0",
    height: "0"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("defs", null, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
    id: "circlePath",
    d: "M150,150 m-100,0 a100,100 0 1,1 200,0 a100,100 0 1,1 -200,0"
  }))));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Theme7);

/***/ }),

/***/ "./src/Components/Common/themes/Theme8.js":
/*!************************************************!*\
  !*** ./src/Components/Common/themes/Theme8.js ***!
  \************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../../../bpl-tools/utils/common */ "../bpl-tools/utils/common.js");
/* harmony import */ var _bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../../../bpl-tools/utils/functions */ "../bpl-tools/utils/functions.js");



const Theme8 = ({
  attributes,
  ReusableRichText,
  setAttributes
}) => {
  const {
    members = [],
    options = {},
    isLinkNewTab = "false",
    isSocial = true,
    isTitle = true
  } = attributes || {};
  const {
    isShowIcon = true,
    icon
  } = options;
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("section", {
    className: "tsbTeamMembersWrapperTheme-8"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "tsbTeamMembersTeamContainer"
  }, members.map((member, index) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    key: index,
    className: "tsbTeamMember"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "tsbTeamMember_img_wrapper"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
    src: member?.photo?.url || "https://images.pexels.com/photos/2379005/pexels-photo-2379005.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=1",
    alt: member?.name
  })), isSocial && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("a", {
    href: member.social[0].link,
    target: isLinkNewTab ? "_blank" : "_self",
    rel: "noreferrer",
    className: "social-icon"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("i", {
    className: member.social[0].icon.class
  })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "tsbTeamMember_content_wrapper"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
    href: "#",
    className: "tsbTeamMember_content"
  }, ReusableRichText ? (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
    className: "tsbTeamMemberName",
    tagName: "h3",
    value: member?.name,
    placeholder: "name...",
    onChange: value => setAttributes({
      members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__.updateData)(members, value, index, "name")
    })
  }) : (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h3", {
    className: "tsbTeamMemberName",
    dangerouslySetInnerHTML: {
      __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__.sanitizeHTML)(member?.name)
    }
  }), !ReusableRichText && isTitle && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
    className: "tsbTeamMemberTitle",
    dangerouslySetInnerHTML: {
      __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__.sanitizeHTML)(member?.title)
    }
  }), ReusableRichText && isTitle && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
    tagName: "p",
    className: "tsbTeamMemberTitle",
    value: member?.title,
    placeholder: "Title...",
    onChange: value => setAttributes({
      members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__.updateData)(members, value, index, "title")
    })
  }), isShowIcon && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", null, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("a", {
    style: {
      display: "block",
      textDecoration: "none",
      outline: "none",
      boxShadow: "none"
    },
    href: member?.icon,
    target: isLinkNewTab ? "_blank" : "_self",
    rel: "noreferrer"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
    dangerouslySetInnerHTML: {
      __html: icon
    }
  })))))))));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Theme8);

/***/ }),

/***/ "./src/Components/Common/themes/Theme9.js":
/*!************************************************!*\
  !*** ./src/Components/Common/themes/Theme9.js ***!
  \************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../../../bpl-tools/utils/common */ "../bpl-tools/utils/common.js");
/* harmony import */ var _bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../../../bpl-tools/utils/functions */ "../bpl-tools/utils/functions.js");



const Theme9 = ({
  attributes,
  ReusableRichText,
  setAttributes
}) => {
  const {
    members = [],
    isLinkNewTab = "false",
    isSocial = true,
    isTitle = true
  } = attributes || {};
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("section", {
    className: "tsbTeamMembersWrapperTheme-9"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "tsbTeamMembersTeamContainer"
  }, members?.map((member, index) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    key: index,
    className: "tsbTeamMember"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "tsbTeamMember_img_wrapper"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
    src: member?.photo?.url || "https://images.pexels.com/photos/774909/pexels-photo-774909.jpeg?auto=compress&cs=tinysrgb&w=400",
    alt: "Sarah Chen"
  }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "tsbTeamMember_social_wrapper"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "tsbTeamMember_social"
  }, isSocial && member?.social?.map((social, i) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("li", {
    style: {
      listStyle: "none"
    },
    key: i
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("a", {
    className: "tsbMember_social_link",
    style: {
      outline: "none",
      textDecoration: "none"
    },
    href: social?.link,
    target: isLinkNewTab ? "_blank" : "_self",
    rel: "noreferrer"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("i", {
    className: social?.icon?.class
  }))))))), ReusableRichText ? (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
    value: member?.name,
    className: "tsbTeamMemberName",
    placeholder: "name...",
    tagName: "div",
    onChange: v => setAttributes({
      members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__.updateData)(members, v, index, "name")
    })
  }) : (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "tsbTeamMemberName",
    dangerouslySetInnerHTML: {
      __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__.sanitizeHTML)(member?.name)
    }
  }), isTitle && ReusableRichText && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ReusableRichText, {
    value: member?.title,
    className: "tsbTeamMemberTitle",
    tagName: "div",
    placeholder: "title...",
    onChange: V => setAttributes({
      members: (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_2__.updateData)(members, V, index, "title")
    })
  }), isTitle && !ReusableRichText && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "tsbTeamMemberTitle",
    dangerouslySetInnerHTML: {
      __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_1__.sanitizeHTML)(member?.title)
    }
  })))));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Theme9);

/***/ }),

/***/ "./src/style.scss":
/*!************************!*\
  !*** ./src/style.scss ***!
  \************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
// extracted by mini-css-extract-plugin


/***/ }),

/***/ "react":
/*!************************!*\
  !*** external "React" ***!
  \************************/
/***/ ((module) => {

module.exports = window["React"];

/***/ }),

/***/ "react-dom":
/*!***************************!*\
  !*** external "ReactDOM" ***!
  \***************************/
/***/ ((module) => {

module.exports = window["ReactDOM"];

/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/compat get default export */
/******/ 	(() => {
/******/ 		// getDefaultExport function for compatibility with non-harmony modules
/******/ 		__webpack_require__.n = (module) => {
/******/ 			var getter = module && module.__esModule ?
/******/ 				() => (module['default']) :
/******/ 				() => (module);
/******/ 			__webpack_require__.d(getter, { a: getter });
/******/ 			return getter;
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry needs to be wrapped in an IIFE because it needs to be isolated against other modules in the chunk.
(() => {
/*!*********************!*\
  !*** ./src/view.js ***!
  \*********************/
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var react_dom__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react-dom */ "react-dom");
/* harmony import */ var react_dom__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(react_dom__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _style_scss__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./style.scss */ "./src/style.scss");
/* harmony import */ var _Components_Common_Style__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./Components/Common/Style */ "./src/Components/Common/Style.js");
/* harmony import */ var _Components_Common_themes_Theme5__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./Components/Common/themes/Theme5 */ "./src/Components/Common/themes/Theme5.js");
/* harmony import */ var _Components_Common_themes_Theme6__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./Components/Common/themes/Theme6 */ "./src/Components/Common/themes/Theme6.js");
/* harmony import */ var _Components_Common_themes_Theme7__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./Components/Common/themes/Theme7 */ "./src/Components/Common/themes/Theme7.js");
/* harmony import */ var _Components_Common_themes_Theme8__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./Components/Common/themes/Theme8 */ "./src/Components/Common/themes/Theme8.js");
/* harmony import */ var _Components_Common_themes_Theme9__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./Components/Common/themes/Theme9 */ "./src/Components/Common/themes/Theme9.js");
/* harmony import */ var _Components_Common_themes_Theme10__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./Components/Common/themes/Theme10 */ "./src/Components/Common/themes/Theme10.js");
/* harmony import */ var _Components_Common_themes_Theme11__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./Components/Common/themes/Theme11 */ "./src/Components/Common/themes/Theme11.js");
/* harmony import */ var _bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ../../bpl-tools/utils/common */ "../bpl-tools/utils/common.js");












document.addEventListener('DOMContentLoaded', () => {
  const teamEls = document.querySelectorAll('.wp-block-tsb-team');
  teamEls.forEach(teamEl => {
    const attributes = JSON.parse(teamEl.dataset.attributes);
    //  const pipecheck = teamEl.dataset.pipecheck ?? false;
    const isPremium = tsmbpipecheck;
    const {
      members = [],
      columns,
      layout,
      isLinkNewTab,
      isTitle,
      isSep,
      isBio,
      isSocial,
      theme
    } = attributes;
    const {
      desktop,
      tablet,
      mobile
    } = columns || {};

    // 			const handlePricinnNavigate = () => {
    //         window.open(
    // 			'wp-admin/edit.php?post_type=tsb&page=team-section-dashboard#/pricing',
    //                                 '_blank',
    //                                 'noopener,noreferrer'
    //                             );
    //   };
    const themeSwitch = theme => {
      if ("theme5" === theme) return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Components_Common_themes_Theme5__WEBPACK_IMPORTED_MODULE_4__["default"], {
        attributes: attributes
      });
      if ("theme6" === theme) return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Components_Common_themes_Theme6__WEBPACK_IMPORTED_MODULE_5__["default"], {
        attributes: attributes
      });
      if ("theme7" === theme) return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Components_Common_themes_Theme7__WEBPACK_IMPORTED_MODULE_6__["default"], {
        attributes: attributes
      });
      if ("theme8" === theme) return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Components_Common_themes_Theme8__WEBPACK_IMPORTED_MODULE_7__["default"], {
        attributes: attributes
      });
      if ("theme9" === theme) return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Components_Common_themes_Theme9__WEBPACK_IMPORTED_MODULE_8__["default"], {
        attributes: attributes
      });
      if ("theme10" === theme) return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Components_Common_themes_Theme10__WEBPACK_IMPORTED_MODULE_9__["default"], {
        attributes: attributes
      });
      if ("theme11" === theme) return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Components_Common_themes_Theme11__WEBPACK_IMPORTED_MODULE_10__["default"], {
        attributes: attributes
      });
    };
    (0,react_dom__WEBPACK_IMPORTED_MODULE_1__.createRoot)(teamEl).render((0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Components_Common_Style__WEBPACK_IMPORTED_MODULE_3__["default"], {
      attributes: attributes,
      id: teamEl.id
    }), ["default", "theme1", "theme2", "theme3", "theme4"].includes(theme) && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: `tsbTeamMembers ${layout || 'vertical'} columns-${desktop} columns-tablet-${tablet} columns-mobile-${mobile}`
    }, members.map((item, index) => {
      const {
        photo,
        name,
        title,
        bio,
        social = []
      } = item;
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
        key: index,
        className: "tsbMember",
        id: `tsbMember-${index}`
      }, photo?.url && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
        className: "memberPhoto",
        src: photo.url,
        alt: photo?.alt
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
        className: "memberDetails"
      }, name && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h4", {
        className: "memberName",
        dangerouslySetInnerHTML: {
          __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_11__.sanitizeHTML)(name)
        }
      }), isTitle && title && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
        className: "memberTitle",
        dangerouslySetInnerHTML: {
          __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_11__.sanitizeHTML)(title)
        }
      }), isSep && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
        className: "memberSeparator"
      }), isBio && bio && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
        className: "memberBio",
        dangerouslySetInnerHTML: {
          __html: (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_11__.sanitizeHTML)(bio)
        }
      }), isSocial && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("ul", {
        className: "memberSocial"
      }, social?.map((socialItem, socialIndex) => {
        const {
          link,
          icon
        } = socialItem;
        const safeUrl = (0,_bpl_tools_utils_common__WEBPACK_IMPORTED_MODULE_11__.sanitizeURL)(link) || "#";
        return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("li", {
          key: socialIndex,
          className: `icon icon-${index}-${socialIndex}`
        }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("a", {
          href: safeUrl,
          target: isLinkNewTab ? '_blank' : '_self',
          rel: "noreferrer"
        }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("i", {
          className: icon?.class
        })));
      }))));
    })), !isPremium && !["default", "theme1", "theme2", "theme3", "theme4"].includes(theme) ? (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "tsm-premium-slider-wrapper"
    }, themeSwitch(theme), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "tsm-premium-overlay"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "tsm-premium-overlay-content"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", null, "This theme is available in the Pro version."), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("a", {
      href: "/wp-admin/edit.php?post_type=tsb&page=team-section-dashboard#/pricing",
      target: "_blank",
      className: "tsm-upgrade-btn"
    }, "Upgrade to Pro")))) : themeSwitch(theme)));
    teamEl?.removeAttribute('data-attributes');
  });
});
})();

/******/ })()
;
//# sourceMappingURL=view.js.map